# Progress Board

Agile-style tracking: epics map to the architecture layers in `CLAUDE.md`. Every story carries a Definition of Done (DoD) — a story isn't "Done" until its DoD is checked off during the self-check step (see `CLAUDE.md` → Working Protocol).

Status values: `Backlog` / `In Progress` / `Blocked` / `Done`.

**State as of 2026-09-18 (full board audit, commit after `74dbc40`).** Suites on a fresh port: combat 374, derivation 258, creature 139, availability 39, all passing; 27 modules parse. **Re-run 2026-09-19 after the class pass: combat 406, derivation 347, creature 140, availability 46, character generation 46, all passing; 33 modules parse.** **After the levelling pass the same day: advancement 77 as well, all six suites passing, and 38 modules parse.** **After the bug-fix pass that followed: advancement 86 and a seventh suite, `tools/levelup-walk.html` (34), which drives the real writing code against a stub actor. Seven real defects fixed, the worst a duplicate class-skill grant on every title advance; see `DECISIONS.md` 2026-09-19 'Bug-fix pass'.** **After the backlog pass the same day: combat 411, derivation 364, creature 145, advancement 101, character generation 54, availability 46, level-up walk 37 -- 1,222 checks over seven suites, all passing, 38 modules parse.** **Three Backlog rows were substantially built and have moved on**: Attributes, now **Done** after its one gap, the language allowance, was built the same day (derivation now 275); Equipment, also **Done** the same day (his encumbrance ported whole, plus the book's movement penalty beside it) and Races & Classes (GME, the good/evil class split, racial ability mechanics). **The pack-compile row was re-described**, because a runtime importer replaced the compile step. **The attribute-maximum row was corrected** for the 25/27 split he confirmed. **The one module with nothing built is Character Generation.** Every "Not verified" note on this board still reduces to the same thing: no Foundry V14 install has run any of this. **The installable system is built at `dist/imagine-rpg/` (2026-09-19): 79 files, 4,384 documents, assembled and self-checked by `tools/build_system.py`, which proves every path the shipped code references is present in the output.** **Prepared for 2026-09-19: `docs/FIRST-RUN.md` records every V14 API path checked against the published documentation (all current), the two defects that audit found, the five things that cannot be verified without running it, and a ten-step smoke test ordered so each failure is the smallest one left.**

**First real install, 2026-09-20 (Daryl).** The system installed and the importer ran, and it found four things no amount of reading could have. **Two were fatal to the import:** `damageAltMode` on weapons and `advancement.classSkillList.requires` on classes are `StringField`s whose `choices` include `""`, and Foundry sets `blank: false` implicitly the moment `choices` is given — so every weapon and every class failed validation with "may not be a blank string" and neither pack imported. Nine such fields across five data models now declare `blank: true` explicitly; a sweep of all 4,389 documents against every declared `choices` list confirms nothing else can fail this way. **One was cosmetic but disabling:** Foundry V13+ paints form controls from its own theme variables, so on a dark-theme install every field on these cream sheets was black-on-cream and invisible until selected, and description boxes were too small to show an entry like "Arch Physical" and could not be resized. All fifteen Imagine applications now declare `themed`/`theme-light`, with explicit paper colours behind that, and a description box spans its section and drags taller. **One was a missing feature:** the race item sheet showed a race's numbers and none of what it *gives* — no racial skills, abilities, colours or ages, which is why Nixie's "water animals only" note was nowhere to be seen even though it was stored and the character sheet already displayed it. That block now exists, in the same chip markup the character sheet uses. **And one was a genuine content gap he spotted by eye:** seven playable races were never extracted — see `DECISIONS.md` 2026-09-20. Races go **105 → 110**; the remaining two (Famorian, Formless) need runtime logic and are a story of their own below. **Not verified:** none of these fixes has been seen in a running V14; that is the next install.

**A fifth from the same install: no sheet window could scroll.** Reported as "all windows need scroll bars", and it was every sheet in the system — anything below the fold was unreachable except by tabbing to it. The near-miss diagnosis is that the three windows in `module/apps/` declared `scrollable` on their parts and the twelve sheets did not; the real one is that `scrollable` only persists scroll POSITION across a re-render and makes nothing scroll. Both halves are now in place: `scrollable` on all nine item bodies and all nine actor tabs (worth having on its own, since these sheets re-render on every keystroke), and the CSS that actually bounds the part's height so it can overflow — `min-height: 0` being the line without which a flex child never shrinks and never scrolls. A horizontal scrollbar went too: `.panel-row` was `repeat(3, 1fr)`, demanding 745px of panels inside a 640px window, and now wraps. See `DECISIONS.md` 2026-09-20.

**Handedness is rolled rather than chosen, 2026-09-20, and the version now moves.** Asked for by the user, and it turns out to be what his sheet always did: `determineHandedness` rolls d100 on race application (1-75 right, 76-95 left, 96-100 ambidextrous, an Ambidextrous race always is) and never offers a choice — the port's two dropdowns were inventing a decision the game does not have. Default off means rolled, in the generator and on the sheet; a world setting ("Players may choose handedness") brings the dropdown back. A die on the Combat tab covers characters made by hand, which never pass through the generator. Creatures stay selectable. Separately: the `damageAltMode` error was reported a second time **from a stale install** — the fix was in `dist/` and nothing else writes that field — so `system.json` goes to **0.2.0** (it had said 0.1.0 since it was created) and the init line now prints the running version, making "which build is this" answerable from the console. See `DECISIONS.md` 2026-09-20.

**Class qualification made visible, and a theme switch, 2026-09-20.** "Cannot select any class but GME" turned out not to be a broken list: `checkClassQualification` was doing its job and saying nothing about it until after a class had been picked. Driving the real modules, a Nixie with rolled attributes qualifies for **43 of 103** classes and a Nixie with 10s across the board qualifies for **1** — GME, the only class with no attribute requirement — which reproduces the report exactly. Each option now carries its own shortfall ("Acrobat — needs STR 13") and a line says how many are open; nothing is hidden or disabled, because the override tick is meant to reach exactly these. Separately, table colours were reworked (alternating rows, a distinct hover, a lighter hairline, sticky headers) and a **client** setting `Sheet colours` offers "Foundry standard" beside the default "Imagine paper" — twenty lines, because every colour was already named in one place. That work exposed a CSS definition cycle between the two halves of the theming, which rendered as black text on a transparent sheet; see `DECISIONS.md` 2026-09-20.

**And the other three windows, same day.** "Need to scroll up and down too on every page" -- the character generator, Level Up and Content Availability were left out of the sheets' scrolling fix on the grounds that each already scrolled an inner body, which was true on paper and false on screen. The generator sized its body with `height: 100%` against a parent with no definite height; the other two named a `scrollable` part, which only persists scroll position. All three now get the same bounded-flex shape the sheets got, with headers and footers staying put. Verified in a 420px window: 908px of generator content through 330px, 1068px of Level Up through 364px, 552px of Availability through 383px, none scrolling sideways. See `DECISIONS.md` 2026-09-20.

**Updates now ship through Foundry's own updater, 2026-09-20.** `system.json` declared `manifest` but not `download`, so Foundry had somewhere to check and nowhere to fetch from and the update button never appeared. Both are declared now, pointing at the manifest and the archive on the repository's main branch, so `git push` publishes an update and an installed system offers it from the Game Systems tab. The zip is committed on purpose (0.5 MB, with a `.gitignore` exception) so manifest and archive travel in one commit and cannot disagree; it is built on every build rather than on `--zip`; and the build now warns when shipped files changed without a version bump, because Foundry compares version numbers and nothing else and an unbumped release is invisible to every existing install. Version is **0.3.0**. One manual replacement is still needed for any install older than that, which has no `download` of its own. See `DECISIONS.md` 2026-09-20.

**Daryl's testing pass, 2026-09-20, version 0.4.0.** Six fixes from his log. The one worth knowing: a tab went BLANK whenever anything on it was added or deleted, on every tab of both actor sheets, because our templates hardcoded `class="tab ..."` and never wrote the `active` class Foundry hands them -- `changeTab` puts it on only on a CLICK, so it survived until the next re-render and no further. Also: rolling height did nothing (`rollPhysique` was passed `#state.raceNames`, which does not exist -- race names live on the derived object); "Remove all" deleted instead of unequipping and is now "Take everything off"; "Starting age 16-5000" was the race's range shown where a character's age was expected; handedness could still be re-rolled, which defeats rolling it; and colours can now be rolled from his per-race lists. Nixie's racial skills were checked against his dictionary and are correct. Starting money is transcribed but NOT built -- one line of his Fortune check is inverted against every other percentile check in the sheet, which is `UPSTREAM-ISSUES.md` item 40. See `DECISIONS.md` 2026-09-20.

**Slight Physique now reaches the race, 2026-09-20, version 0.5.0.** Daryl answered UPSTREAM-ISSUES item 38: Slight Physique is a free choice on his sheet, not a gender, and faerie wings follow it. The port had carried the tick since the generator was built and it only ever did −1 STR / +1 AGL; the extractor had both branches of every inline race row filed under `_slightPhysique` waiting for the answer. The two are joined now — both forms ship on the race document and the tick picks between them. The wingless form is a trade: Fairy gains Climb and Cover Tracks, Fairy(Dark) gains Climb and Wood Lore +10% (the "Wood Lore is incorrect" he reported), Podling and Sporeling gain nothing, and Gremlin flies either way while its ORDINARY form gains Climb. Separately, the Dark Fairy's missing Iron Aversion and Night Vision are a gap in his own table and were NOT invented — item 41. See `DECISIONS.md` 2026-09-20.

**The three optional starting-kit rules, 2026-09-20, version 0.6.0.** Gear by culture (wilderness kit by race, instead of coins), clothing by status (race, social class, gender, style) and tools by skill (each social skill brings its own). All three off by default and ticked in the generator, as on his sheet. Parsed rather than transcribed — 1,230 lines of switch across seven functions — by `tools/extract/extract_starting_kit.py`, which also found that five of the six wilderness kits have bands with no `break`: social 5 and social 12/13 silently take the NEXT band's gear. That is `UPSTREAM-ISSUES.md` item 42, reproduced faithfully and reported on every extraction run. See `DECISIONS.md` 2026-09-20.

**Set Trap's variants, 2026-09-21, version 0.7.0.** Daryl reported Set Trap missing from the skill compendium; the bare row was there, and he was right anyway. Set Trap, Detect Trap and Remove Trap are each ONE row in his `skilldict` and TWO entries in his `skilllist` — a wilderness form and an urban one — and the port built from the dictionary, so the six variants never existed. His class lists name a variant **73 times** and the bare form never; racial lists name one 33 times. The variants are now built from the base rows (skills 674 → 680), and `check_skill_references` follows all 472 skill names a class or race grants on every extraction run, which is the part that was missing. Four remain unresolved and are his: `Call of Fire`, `Divine Knowledge(w)`, `Cover Track` and `Plant Speak` (a spell, not a skill). `UPSTREAM-ISSUES.md` item 43.

**An item picker, 2026-09-21, version 0.8.0.** "The items menu is empty, so I can't add items to my character" — both true. Content lives in compendia and nothing is put in Foundry's Items sidebar (deliberate: 4,389 documents would bury a Game Master's own), but the Equipment tab's Add buttons made a blank "New Weapon" rather than offering the 594 real ones, so dragging from an open compendium was the only route and nothing said so. The Add buttons now open a searchable picker over the pack for that gear type, honouring availability, reading the INDEX rather than the documents, and staying open so a character can be kitted out in one go. The blank item is still one click away inside it. See `DECISIONS.md` 2026-09-21.

**The Items sidebar is populated, 2026-09-21, version 0.9.0.** Asked for directly, reversing the earlier call to leave it empty: `game.imagine.populateItems()` fills Foundry's Items directory with all 4,395 documents in 86 folders, `clearItems()` takes it out. His groupings where he has them (weapon type, armour flexibility plus shields, skill category then sourcebook, classType); initial-letter buckets where his tables carry no category at all. Missile is 408 weapons and splits on his own signal — a weapon with no speed is ammunition, and each names its launcher in its brackets, giving 26 groups of about 24. Making folders out of categories surfaced two data faults: Beguiler's classType and description are SWAPPED (item 44, left alone pending his word, as the Monk repair was), and two slot-marker skills carry "?" as a sourcebook. See `DECISIONS.md` 2026-09-21.

**Wood Lore off the Dark Fairy, 2026-09-21, version 0.10.0.** Asked for directly. Done through the manual layer (`src/packs/manual/races.json`, `_override: true`) rather than by editing the extraction, because his own `raceSkillDetailValues` gives the wingless form Climb and Wood Lore +10% and Daryl himself described that rule — so this overrides his data rather than correcting it, and the two stay distinguishable. Only `racialSkills` is laid over; every build prints the override. Climb was left, since only Wood Lore was asked for. See `DECISIONS.md` 2026-09-21.

**The Items sidebar reads the compendia, 2026-09-21, version 0.10.1.** Daryl: gear was in the compendium but not the Items list. `populateItems()` had read the JSON shipped with the system rather than the world's compendia, so anything added to a compendium by hand never reached the sidebar; it now reads `world.imagine-*` first and falls back to the shipped file only when a pack is missing or empty. A batch Foundry refuses is now reported (console, a warning and a `failed` list in the return value) instead of throwing out of the whole run and silently skipping every folder after it. Checked with a mocked Foundry over the real content: homebrew gear lands in Equipment, a failed Classes batch does not stop the other folders, a re-run adds nothing twice. NOT reproduced against a real V14 world, so if the shipped equipment was also missing, the console line per folder now says why. See `DECISIONS.md` 2026-09-21.

**Every item says where it came from, 2026-09-21, version 0.11.0.** Asked for directly: find the source and page for all the items. Only his `SKILLDICT` carries a sourcebook and a page -- which is why a skill's sheet read "Player's Guide 147" and a weapon's showed two empty boxes -- so 3,715 documents shipped blank. **His own Master Index answers it**: `Nearly-master-index.pdf`, version 0.976, his cross-book consolidation, whose tables carry a `Source` column naming the originating book. New `tools/extract/extract_sources.py` reads it into `src/packs/named/itemSources.json` (committed, because the book text is not); `build_documents.py` applies it. **2,693 of 4,395 documents now name a source** -- 983 a book of his, 1,710 his Master Index -- and 1,702 carry `XXX`, shown in red on the sheet, listed in `docs/UNATTRIBUTED.md`. Ground-truthed against the 680 skills whose answer he already gave: **98% correct, 662 of the 670 it committed to**. Angel Sword, which is in none of the four books on disk, is a Heroic Melee Weapon on Master Index 326. See `DECISIONS.md` 2026-09-21.

**Legends of the Unknown, and a generated gap report, 2026-09-21, version 0.11.1.** He supplied Legends of the Unknown (465 pages, extracted, index at 462-465 offset 6), which confirmed 23 more page numbers but did not move the XXX count -- his Master Index already named the book for that content, so the book adds the page rather than the attribution. **Conquest of the Eternal may be unreleased**; the 100 documents citing it are named but not page-referenced, and `UNRELEASED_BOOKS` in `build_documents.py` now says so at the top of the gap report. `docs/UNATTRIBUTED.md` is written by `build_documents.py --write` -- it had claimed to be rebuilt every time while actually having been produced once by a throwaway script. See `DECISIONS.md` 2026-09-21.

**The races his sheet splits with a second dropdown are races of their own, 2026-09-21, version 0.12.0.** Asked for directly, with Michael's reasoning: the winged faeries need a split type tied to Slight Physique, and Maginos should split by build material. Five of his races are ONE dictionary entry and more than one race a character can be, because his sheet asks a SECOND question beside the race picker — `maginos_material`, and the slight-physique tick. A Foundry race is an Item and cannot ask a question, so each form is now a document, named as his class paths are: **Fairy(Winged), Fairy(Wingless), Fairy(Dark Winged), Fairy(Dark Wingless), Podling(Winged/Wingless), Sporeling(Winged/Wingless), Maginos(Clay/Metal/Stone/Wood)**. **The wings ARE the slight physique in his own code** — his slight branch sets "Fly:" where his ordinary branch sets "None:" — so `physiqueLock` forces the generator's tick and hides it: a winged Fairy gets −1 STR / +1 AGL unticked, a wingless one never does however hard it is ticked. **Gremlin is deliberately not split** (it flies either way; its ordinary form gains Climb) and keeps its free tick. The four Maginos material rows had been extracted for months and silently dropped on the floor. `sourceRace` carries each form to his name-keyed tables rather than any name-parsing. **Races 110 → 118.** Two real bugs caught on the way: two forms of one race could no longer breed with each other (his rule is a race is always fertile with itself), and the Wood Lore override would have become a new Custom race. New `check_race_references` found four more — two malformed names of his (item 45) and the number on the Famorian/Formless gap. Derivation **392** (28 new); eight suites, 1,228 checks, all passing; 41 modules parse. See `DECISIONS.md` 2026-09-21.

**Formless is built, and Daryl's Blocker is fixed, 2026-09-21, version 0.13.0.** **Formless** was never a row — but half of it is a literal, and that is what made it buildable. His case at 33625 supplies the whole MENTAL half (INT/WIS/KNW/CHM/AUR/PTY/WIL, the per-title Endurance roll, Affinity, Fortune, the five resistances) and `setFormlessStartingRace` supplies the PHYSICAL half from a host race; between them all twelve attributes are covered once each and nothing twice. So the character holds **two race items**, as a Half Race does, and `combineFormless` takes each half whole rather than averaging — his own code refuses the half-race path for a Formless outright. **110 hosts**, his 102 plus the four faeries that are now eight. A hostless Formless, a Formless in a Formless, a second body and a host his sheet does not offer are all **reported, never refused**. His second copy of each host's physical half is NOT used: it has drifted from the race table in **22 cells across four races** (item 47) — and that drift is the first real evidence on two long-open questions, giving **0 where item 24's Elf(Sea)/Elf(Ice) speed multiplier is −10**, and a flat 0/4 where item 2's Gaunt carries `0-getDieRoll(4)`. Races **118 → 119**. **Daryl's 0.11.1 Blocker** — no roll buttons on the five resistances — is fixed on both actor sheets, and building it found that **every resistance roll on his own sheet reports "virtually immune"**: his handlers read the chance as a string and `"50" + 0` is `"500"`, which passes his `> 199` test, so any two-digit resistance auto-passes. Verified in a real JS engine; item 48. Also: `split_list` now splits only outside brackets, because one of his Formless disabilities is a sentence with commas in it. Derivation **439** (46 new); eight suites, 1,275 checks, all passing; 42 modules parse. See `DECISIONS.md` 2026-09-21.

**His errata arrived, 2026-09-21, and nothing is built from it yet.** Six books' corrections plus three unpublished tables — 2,384 lines, dated 2025–2026, newer than anything else in hand. It is a **third** source, and `CLAUDE.md`'s "the Roll20 sheet beats a rulebook" was written when there were two, so the precedence question is open and is **the user's to settle**. The finding that reframes it: ten class/race restriction changes were checked against what the port already builds from his sheet and **most were already true there** (Fairies can already be Tricksters, Kenku can already be Mage, Dark Fairies are already barred from Berserker/Bounty Hunter/Martial Artist) — one real difference, Dark Fairy/Monk. So he keeps the sheet current with his errata. Recommended treatment is **errata as a check, not an override**. Full triage, including what is known to be affected (Maginos hide by material, Brownie's STR max, the creature hide cap, undead shadowform), in `docs/ERRATA.md`. The files are **not committed** — his rules content, some unpublished; `.gitignore` now holds the whole `docs/reference/errata/` directory.

**Famorian is built and the race layer is complete, 2026-09-21, version 0.14.0.** The last unbuilt race, and the opposite problem to Formless: a Formless is half a race waiting for a body, a **Famorian is a whole race whose body is built**. His 593-line case divides cleanly — every evoke test is `if (evoke on) {…} else {…}`, so the **else branches together are an ordinary race** (a Famorian that has taken no evokes: −4 Social Class, mental limits 19, beauty/charm 15, social 12, +5 Perception, −10 Affinity, −5 Fortune, +10% magic, **−10% disease**, a 1d4 Endurance roll and no special movement) and the if branches are what each evoke does. **Of ~120 evokes only FIFTEEN change a number**; the other ~105 are described abilities, listed and not applied, as racial abilities already are. Order is his and matters twice — four evokes write the same movement slots and his own comment says "set fins first (swimming is of least importance)", so wings beat legs beat insect legs beat fins. His resistance figures are **absolute, not deltas**: Enhanced Disease Resist advertises +20% and writes 10 over a base of −10. The **breed is rolled** (d100: Hidden 1-10 … True 91-95 = All … Inbreed 96-100) with a setting to choose, as handedness was; "All" is an *absence* of a ceiling, per his own `!= "All"` check. Evokes are an array on the actor, the 1d3 bonuses rolled once and kept. Over budget and a missing animal type are reported, never refused. **A third copy-paste slip of his**, not reproduced: Regeneration(Budding) tests the *Natural* checkbox, so it can never be listed (item 49, after 46 and 45). **Races 119 → 120; every race in his `specieslist` now exists**, and `check_race_references` is down to his two malformed names. Derivation **469** (30 new); eight suites, 1,305 checks, all passing.

**Seven modules were never being parse-checked, 2026-09-21.** `tools/syntax-check.html` holds a hardcoded list (a static page cannot glob), and it had drifted: it reported a cheerful "41 modules" with 48 on disk. Found because two modules written that day did not move the count — but **five of the seven were months old**, including `apps/item-picker.mjs`, `item-directory.mjs` and `sheet-theme.mjs`, all shipped and all reported as working. All 48 are listed now, and `tools/build_system.py` compares the list against `module/` on every build and names anything unchecked or stale. A self-check that quietly stops covering new code is worse than none, because it is trusted.

**Every race says which book it comes from, 2026-09-21, version 0.15.0.** Daryl (Arikail) pushed `patch-1` to his fork: all 110 races attributed by hand to the book they actually come from, with a page on each. Real work, filling a real gap — the port had **87 races pointing at the Master Index**, which names itself as the source, and 14 marked XXX. **It could not be merged as it stood**, for three reasons none of which are about the data: `src/packs/documents/races.json` is GENERATED and the next build erases it; the branch is based on the old main, so merging would have reverted 120 races to 110 and removed ten; and the JSON does not parse — six commas are missing between `"sourcebook"` and `"page"`, so no races would have imported at all. **There was also nothing to approve**: no pull request was ever opened, only a branch on his fork. **So the data was ported and a home was built for it**: new `src/packs/manual/sources.json`, which wins over the generated `itemSources.json` (itself regenerated from the Master Index, so hand edits there are lost too). His 110 names expand onto the port's 120 through the same mechanism every other race-name list uses, so each split form inherits its base race's attribution. The last two were found in his own books: **Famorian = Aspects of the Wild p.6** (that book's contents page), **Formless = Epitaph of the Fallen p.6** (the Epitaph errata's own heading). **All 120 races now carry a real book and page; none is XXX.** Two independent confirmations of the same day's work fell out of it — Aspects gives the Famorian "Perception: +5% Affinity: -10% Fortune: -5%, Starting Endurance +1, Title Bonus (1d4)", exactly the base derived from the else-branches of his sheet; and the Epitaph errata's Formless "Wisdom max 17, Knowledge max 22" is already what his sheet sets. See `DECISIONS.md` 2026-09-21.

**A Famorian can be made in the character generator, and a same-day-old wiring bug is caught before it shipped, 2026-09-22, version 0.16.0.** Closes item 1 of `docs/sonnet/2026-09-21-famorian.md`. Before writing the generator step, a real bug turned up: `_getEffectiveRace` and `_getRaceIssues` read `this.identity.famorian`, but the field lives at `this.physical.famorian` — `identity` has no such field, so every read silently returned nothing. **A Famorian's evokes were never applied at all**, the budget check always trivially passed, and "no animal type" fired even when one was set. Not caught by yesterday's own tests, which drove the rule functions directly rather than through a real actor. **Confirmed the failure mode before fixing it**: reverted, watched the new integration test fail with exactly the predicted numbers, restored, watched it pass — so a repeat of this exact bug cannot pass silently again. **The generator step itself**: a `famorianBreedSelectable` world setting beside handedness, same shape (rolled and standing by default, no re-roll button — "a button that re-rolls until you get what you want is the same as choosing it"); the three attribute evokes roll their 1d3 the moment they're taken and drop the moment they're not; an evoke picker, all ~120, alphabetical, the fifteen numeric ones in bold, with a live budget. Verified as a **full round trip** — generator choices → `assembleCharacter` → a real `ImagineCharacterData` rebuilding the identical race — and the real Handlebars template was compiled and rendered against real content, not just the JS view object inspected. **Two gaps closed on the way**: the Formless host picker's own `formlessIssue` was computed since yesterday and never rendered (a Formless with no host, or a disallowed one, told the player nothing); and `module/chargen-view.mjs` — everything the generator window and its preview both call — had **no automated test anywhere**, only a manual harness and the parse-only syntax check, which is exactly the shape of gap that let the `identity`/`physical` bug happen in the first place. Derivation 484 (36 new), character generation 77 (2 new); nine checks all passing, 48 modules parse. See `DECISIONS.md` 2026-09-22.

**Play-testing feedback on 0.16.0, 2026-09-22, version 0.16.1.** Five reports, three fixed, two pinned. **"Animal Shape: (water animals only)" on almost every race** was not data: it was the grey placeholder of the race sheet's Skill note field, Nixie's note used as an example, and an example in an empty field reads as the race's own. Removed; Nixie is still the only race carrying it. **Plain `Fairy` and `Fairy(Dark)` beside their four forms** were left over from before the split: the importer matches by name, so it adds and updates but never removes a document the source has dropped. A new explicit `RETIRED_DOCUMENTS` list in `module/content-importer.mjs` takes them out on the next import (also `Podling`, `Sporeling`, and the two classes split by path, `Elemental Dancer` and `Innominate`), and the Items-sidebar fill removes them from its own folders. The "Awaiting the developer" text the report quoted was the description of those retired races, and goes with them. **Dark Fairy's Animal Shape may take a D'Wisp's form** -- Aspects of the Wild p.4 -- added as a skill note on both forms through `src/packs/manual/races.json`. His sheet gives the Dark Fairy no Animal Shape skill at all, though; asked in `UPSTREAM-ISSUES.md` item 50. **Pinned by the user:** Formless body-switching (a free-text *Formless: Bodies* field on the Description tab stands in until it is built) and the pre-release clean-up of race descriptions. Both are rows in Epic 2. **Famorian evokes are not limited by animal type, deliberately**: his sheet does not limit them either, and the user's call is that the Game Master polices it. The bestiaries do give each animal an Evoke Package, so it is buildable later; a Backlog row in Epic 2. See `DECISIONS.md` 2026-09-22.

**The Sonnet backlog, worked through, 2026-09-22, version 0.17.0.** All 29 hand-off notes in `docs/sonnet/` were triaged against the code: most items had already been built by later passes, some wait on the developer, and the rest (about 35) were built in nine parallel streams. **Built:** race sheet shows form, source race, Gremlin's slight-physique variant, the Famorian breed table (editable) and evoke list, and edits `formlessHosts`; class sheet shows barred races, class skills, base class/path and Arch Mortal requirements; character sheet shows Famorian evokes and budget, the physique lock, the winged/wingless Half Race conflict and class qualification; the generator equips the best starting armour; item `quality` scales weight; his multi-missile acquisition roll ported as rules (no sheet button yet); `isRestricted` read from the books for 366 skills; equipment grouped by type and an XXX folder in the Items sidebar; `RETIRED_DOCUMENTS` tested, and the build warns when it is incomplete; a choices/blank sweep in the build; a manual-content loader test; a window-chrome harness; the stylesheet's colour literals themed; resistance roll, `sourceRace`/`physiqueLock`/`formlessHosts` documented. **And a What's New window** showing CHANGELOG.md once per user after each update (the user's request). **Two defects found on the way:** source attribution depended on Python's per-run set order, so a rebuild could move an item's page with no code change (now fixed, and three pages improved); and `build_system.py` read git output as cp1252 on Windows. **New for the developer:** `UPSTREAM-ISSUES.md` items 51 (Mixed flexibility) and 52 (racial social-skill bonuses: his table exists, the port lacks it, and his lookup passes an array where a name is expected). **Left, with reasons**, in `docs/sonnet/2026-09-22-sonnet-backlog.md`. See `DECISIONS.md` 2026-09-22.

**The Magic & Lore tab and his starting lore, 2026-09-22 (not released).** Asked for directly: a magic, lores, herbs and potions tab, populated, and the lore generation the port never did. His Magic/Lore tab is now a sixth tab on the character sheet, over **four new item types** (consumable, lore, spell, invocation) and **four new packs, 2,486 documents** built from seventeen of his dictionaries: 515 herbs, potions, elixirs and charms, 961 ballads, candle rituals, empathy and sympathy rituals, glyphs, hymns, poems, rituals, runes, songs and evokes, 550 spells, 460 invocations. Poisons are built from a type and a potency, as his are. The tab shows consumables with a Use (a dose spent, the row gone with the last), each lore the character holds with its learn and use skills, chance and practitioner title, Use/Brew rolls on his rules, memorization against (goal + 1) x Knowledge's points, and spells and invocations known and memorized. **His "Provide random lore" is ported** from his own chain (`tools/extract/extract_lore_tables.py` walks it): an entry per lore skill held, herbs, potions and poisons with doses, starting spells for a caster, on by default in the generator and a Game Master button on the tab for anyone made before. Casting and each entry's worked-out EFFECT are not built (Layer 4, and his `do<Kind>Action` switches). New `tools/lore-test.html` 103, `tools/magic-preview.html` renders it end to end; window test 27, manual content 122; UPSTREAM 57-60. **Not released:** no version bump or `dist/` build, because another session had uncommitted work in the tree while this was done; see `docs/sonnet/2026-09-22-magic-lore-tab.md`. See `DECISIONS.md` 2026-09-22.

**The round clock: his Mr. Initiative chart, for every combatant, 2026-09-22 (not released).** Asked for directly -- "a time tracker for each player: take the init values and modify by seconds until we reach the end of that turn" -- and his Master's Manual already had it, pp.98-100: a board for the 10 second round with a Split Second row, ten seconds, extra seconds that cut a second in two, an off-hand row and a carry-over grid. Now built on the event-time round the tracker already had: every combatant's **clock** (the roll and every spend, in their flags, which a player may write on their own combatant) is drawn **under their row in the combat tracker** -- seconds lost to a late start, spent, left, and where they stand -- with +1, +N (either hand) and undo, and all of them line up in a **Mr. Initiative window** (stopwatch at the top of the tracker, or `game.imagine.roundClock()`). **Carry-over** (Player's Guide p.168): an action running past second 10 finishes next round, whose reaction roll is added to its last second; an initiative past 10 carries the wait; either can be declined for a fresh roll. **Off hand** (p.178): 5 seconds, more with Second Weapon Lore, 10 ambidextrous, less half what a late start lost, never more than the round has left -- the pool the 2026-09-14 pass left open. **Extra seconds**: a roll below -10 (Master's Manual p.2) plus a new `combat.speedSeconds` (his tmp_speed_seconds, entered on the Combat tab or in the window), ten at most; a sped combatant starts first, as his sheet has it. Attack cards spend on the hand the weapon is in, once. A typed or rolled initiative restarts a clock. New `tools/round-test.html` (87) drives the rules against the books' own examples and a whole fight through the real combat document on stubbed Foundry; every other suite unchanged; Foundry API paths checked against the V13 source installed on this machine. UPSTREAM 61 asks two readings. See `DECISIONS.md` 2026-09-22 and `docs/sonnet/2026-09-22-round-clock.md`.

**A poison used on someone, and a weapon mods window, 2026-09-22 (not released).** Asked for directly: "2" (a poison used against a target) "also we need a pop up menu for weapons to apply various other mods (blessed, etc)". **Use on a poison** now asks who takes it (the targets, oneself, or no one), rolls each victim's Poison Resistance and posts his outcome with the onset and duration rolled and the Endurance damage over the whole of it (not applied); durations follow his row and the book (minutes), not his handler's hours (UPSTREAM 62). **A wand on every weapon row** opens the weapon mods window: temporary effects (Bless by the Player's Guide's Holy Weapon, a day, figures by the wielder's alignment -- the user's ruling -- and the Game Master's own, ending by the world clock), and **his whole Customize panel**, weapon half, extracted from his sheet with his suitability rules, applied to the weapon or to a customized copy beside it (the user's table practice). The attack reads it: runes, Blessed, temporary figures, mode abilities and their natural-roll double; Foe Strike, energy, Bane and the divine abilities are written on the card for the table. Rune damage follows his dictionary over his code (UPSTREAM 63). **Every choice also changes the weapon's figures as his combat sheet does** (a Chain Weapon a die more and two seconds slower, a Worn blade -2 and -1 to hit, a +3 two seconds quicker), from his listing-change functions, generated; UPSTREAM 64-65 found on the way. New `tools/weapon-mods-test.html` 69; lore test 117; everything else unchanged. **Not released**, as the two passes before it. See `DECISIONS.md` 2026-09-22 and `docs/sonnet/2026-09-22-weapon-mods.md`.

**Open with the developer:** `UPSTREAM-ISSUES.md` item 2 (Gaunt), item 3 (is the sheet current), item 4 (saves' half-chance vs ±20%), item 5 edge cases, item 10's Enhanced Perception double count, items 28 and 29 (multi-missile), and new items 38 (can a Fairy fly?) and 39 (two slips in the inline race rows).

---

## Epic 0 — Research & Continuity Foundation

| Story | Status | Definition of Done |
|---|---|---|
| Extract & map Roll20 source sheet | Done | Structure understood: 2 actor types (Character/Creature), 10 tabs, 20+ repeating item types, field names cataloged by area. ~~No JS/sheet-workers present~~ — **wrong**, corrected 2026-09-10: the HTML embeds a 180k-line sheet worker, extracted to `docs/reference/sheet-worker.js` (row below). |
| Extract & map Player's Guide | Done | OCR'd to `docs/reference/players-guide-fulltext.txt`. Bookmark structure mapped (Attributes/Races/Characters/Classes/Skills/Combat/Equipment/Magic/Divine Magic/Appendix). Core skill-check and attribute-save formulas pulled and cross-validated against the Roll20 sheet's sample character. |
| Extract & map Master's Manual | Done | OCR'd to `docs/reference/masters-manual-fulltext.txt`. Confirmed as GM-facing expansion (extended attribute ranges, class extensions, expanded combat, magic item design), not a base-rules source. |
| Set up continuity scaffolding | Done | git initialized, `CLAUDE.md`, `docs/DECISIONS.md`, `docs/PROGRESS.md`, `docs/reference/` in place; initial commit made. Verified 2026-09-12: initial commit is `07fd9f9`, all three continuity docs are present and have been maintained since (`DECISIONS.md` and this board are both live), and `docs/reference/` holds six files — the four book extracts, `sheet-worker.js` and `foundry-v14-requirements.md`. `docs/STYLE.md`, `docs/UPSTREAM-ISSUES.md` and `docs/sonnet/` were added later and exceed the original DoD. |
| Obtain Bestiary / Creature source material | Done | *Aspects of the Wild* (176pp, first Bestiary Expansion) and *Mysteries of the Planes* (657pp, fourth) extracted to `docs/reference/`. Aspects is mostly playable races (~18 with full racial ability tables) plus creature-creation guidance rather than stat blocks; Mysteries covers the inner planes. The creature *mechanical* data was already in hand from his JS (creature `abilitylist` 1,114 entries, `disabilitylist` 249, `immunitylist` 149). |
| Obtain magic subsystem source material | Done (superseded) | No longer blocking — magic content data (`rituallist`, `evokedict`, `spellPrimers`) is already present in `sheet-worker.js`. Prose-only gaps may remain. |
| Discover + extract embedded sheet-worker JS | Done | 180,370 lines recovered from the HTML's `<script type="text/worker">` block to `docs/reference/sheet-worker.js`. Corrects the earlier false "no JavaScript" finding. ~37 data dictionaries cataloged; shape confirmed uniform and machine-consistent (470/470 skills at 10 cols, 204/204 social at 8). |
| Confirm `sheet-worker.js` is the dev's current ruleset | Backlog | **Still formally open** (`UPSTREAM-ISSUES.md` item 3). Dev described the code from memory rather than sending it; confirm this file is not a superseded version. **Re-read 2026-09-18:** no longer blocking in practice — Epic 6 went ahead and 4,367 documents are built from it, and on 2026-09-17 he answered nine questions citing its exact line numbers without once saying a cited function was out of date, which is strong circumstantial evidence it is current. Still worth one direct yes/no from him. |

## Epic 6 — Content Extraction Pipeline

| Story | Status | Definition of Done |
|---|---|---|
| Generic dictionary parser (`tools/extract/parse_dictionaries.py`) | Done | Tokenizer-based (not regex) so mixed types, nested arrays and embedded expressions surface instead of corrupting. Extracts **87 dictionaries / 12,595 entries**, with exactly one non-literal value correctly flagged rather than mangled (`raceStatsAndMoveDetails` → `"Gaunt"` contains a live `getDieRoll(4)` call). Raw output in `src/packs/raw/`, checked in for reviewable diffs. Handles his implicit-global declaration style (`classRequirementsAndDetails={`), not just `const`. Duplicate dictionary names (character vs creature `abilitylist` etc.) disambiguated by start line so neither is lost. |
| Per-dictionary column maps | Done (core scope) | 27 dictionaries mapped to named fields in `tools/extract/column_maps.py`, output to `src/packs/named/` — the 21 core ones plus the six trait dictionaries (his racial and creature copies of abilities, disabilities and immunities, all uniformly four columns). Maps are sourced three ways and each records which: his own column-header comments, derivation from how the code consumes the row (`tools/extract/derive_column_maps.py` reads `setAttrs({field: row[N]})` patterns — this is how the unlabelled 62-column race table and 22-column class table were recovered), or self-evident structure. `map_columns.py --check` treats width mismatches as errors, which caught four maps I had inferred from the rulebooks rather than the code. ~~20 of 21 clean~~ **21 of 21 clean** since 2026-09-17, when the Monk row repair he authorised landed (row below). Remaining dictionaries (magic subsystems, martial arts, armour sub-tables) still need maps. |
| Report the Monk data defect to the dev | Done | Reported as `UPSTREAM-ISSUES.md` item 1 and **answered 2026-09-16**: *"sounds like the right fix"*. `classRequirementsAndDetails` → `"Monk"` had 21 columns where all 87 other classes have 22 — 4 `classMod` slots instead of 5, shifting armour usage, weapon usage, class modifier, title name, attribute qualifications and casting one place left, so Monk's armour usage displayed its weapon list. Now repaired on load: `column_maps.py` gained a `ROW_REPAIRS` table holding the row, the repair and the answer that authorised it, the blank fifth slot is inserted at index 15, and every repair is printed on every run. The dictionary maps 88 rows clean where it mapped 87 and dropped one, and Monk builds with its fields in the right columns. This and the insect thorax multipliers (item 9) are the only two places the extraction edits his data, both on his explicit confirmation. |
| Resolve the one non-literal value | Backlog | `raceStatsAndMoveDetails` → `"Gaunt"` uses `0-getDieRoll(4)` where every other race has a literal. Decide whether this is a random stat by design or a bug in his sheet; ask the dev. Still open as of 2026-09-18 (`UPSTREAM-ISSUES.md` item 2) — it was not among the nine he answered. |
| Generate `src/packs/*.json` for core content | Done | `tools/extract/build_documents.py` shapes the named extraction into Foundry documents in `src/packs/documents/`: **4,367 documents** across skills (674), weapons (594), armour (719), equipment (636), races (105), classes (87), abilities (1,154), disabilities (249) and immunities (149). The three trait packs were added after the creature actor, so its abilities can be dragged from a compendium rather than typed; each category is its own pack because 19 names appear in two categories at once and documents are matched by name. Both of his copies of each trait dictionary are read, and where they disagree the creature row is kept — 41 such conflicts and 40 racial-only entries, reported every run and matching `UPSTREAM-ISSUES.md` item 10. Classes merge three dictionaries keyed by name, plus a manual layer: five classes (Elemental Dancer, Elementalist, GME, Inquisitor, Summoner) are in his `classtitledict` and `goalupdict` but have no `classRequirementsAndDetails` row at all, so nothing could be built for them from the sheet-worker — `UPSTREAM-ISSUES.md` item 22. **Elemental Dancer is now authored** in `src/packs/manual/classes.json` from his own Word class template, with his code still winning every field it carries and both disagreements recorded; a manual entry never overwrites a class his data can build. `advancement.classSkills` is a new field holding the template's per-title skill progression, which his dictionaries do not carry. **Monk now builds too** — he confirmed the column repair (item 1), so the pipeline inserts the missing slot and the class documents rebuild at 88. **And the five-class gap was our mistake, not his**: he checked and *"the data is there. I can see it on the sheet when I try to make a Roll20 character of those types"* — the port had looked only in `classRequirementsAndDetails` and read absence into it. GME turns out not to be a class at all (any social and racial skills, one at a time, 0-title and no racial title-1 bonuses) and Elementalist, Summoner and Inquisitor split on a good/evil choice that changes their skills, which he is happy to see modelled either as a choice field or as one document per path. Both are real work items now rather than a reported gap; item 22 carries his wording. Conversion is 99.75% clean; the 7 remaining edge cases are reported every run and listed in `docs/UPSTREAM-ISSUES.md` item 5. |
| Compile JSON -> Foundry compendium packs | In Progress | ~~`packs/` builds from `src/packs/`~~ — **the approach changed and the row was never updated** (found 2026-09-18). Packs are not compiled at build time: `module/content-importer.mjs` (commit `cd57036`) builds nine world compendia at runtime from `src/packs/documents/*.json`, idempotently and matched by name, exposed as `game.imagine.importContent()`. This needs no Node toolchain and lets whoever runs the game refresh content when he sends a corrected sheet. So `system.json` declares `"packs": []` on purpose, and `packs/README.md` has been rewritten to say so, replacing the stale "Empty until Layer 0's content layer design...". **Remaining:** the import has never run in a real Foundry V14 install, and that is the whole of what is left. |
| Adding content to every pack without code | Done | **Built 2026-09-19 at the user's direction ("leave a method of adding new content to every module").** There are two routes, both in `docs/ADDING-CONTENT.md`. (1) **In Foundry:** a Game Master authors items in a compendium of their own and tags a Sourcebook. The Content Availability window already discovers sourcebooks from every Item compendium, so the tag becomes an on/off switch by itself with no new code. The guide warns that the system's own compendium entries are overwritten on re-import, so stock entries are changed by copying, never by editing. (2) **In the content files:** one `src/packs/manual/<pack>.json` for each of the nine packs, each with a worked `_example`, merged by one shared `apply_manual_content` that replaced the classes-only loader. It is additive by default. Overriding his data needs `"_override": true` and is reported on every build. Hand content defaults to sourcebook "Custom". A misspelt field is reported against the item's schema. Tested with every example, a typo and two collisions; with the shipped (empty) files the documents are byte-identical. **Not verified:** route 1 in a running V14. **Known gap:** the race and class item sheets cannot yet edit their list fields (`docs/sonnet/2026-09-18-races-classes.md` item 1). |
| Extend extraction to magic-phase dictionaries | Done | **Done 2026-09-22 for the seventeen his Magic/Lore tab reads**: column maps from his header comments for herbs, potions, elixirs, charms, ballads, candle lore, empathy magic, glyphs, hymns, poems, rituals, runes, songs, sympathy magic, evokes, spells and invocations (`column_maps.py`, 54 dictionaries clean), built into the consumables, lore, spells and invocations packs (2,486 documents), and his starting-lore chain, poison switches and starting-spell ladders walked into `module/lore-tables.mjs`. Still unmapped: the magic-item and divine-item empowering dictionaries, which belong with the enchanting subsystems. Original DoD: `rituallist`, `evokedict`, `spellPrimers` and remaining subsystems extracted. |
| Races assigned inline rather than from the dictionary | Done | **Updated 2026-09-21: the five that ship are now twelve, and both halves of every split form are used.** The four faeries were replaced by a winged and a wingless document each (his slight branch is the winged one, and the wings ARE the slight physique, so `physiqueLock` ties the generator's tick to the race), and the four Maginos material rows — extracted for months and dropped on the floor by a local variable nothing read — became races of their own beside the bare Maginos, which stays as his `[Other]` material. Races 110 → 118. **DONE 2026-09-21: Formless and Famorian are both built, and every race in his `specieslist` now exists as a document (120).** Both were specified first, then built the same day: `docs/sonnet/2026-09-21-race-forms.md` carries the breed table, the evoke budget and the body-chart effect for one, and the literal mental block, the host mechanics and the 36-column host dictionary for the other. `check_race_references` now reports the gap with a number on it: **7 classes bar Famorian and 11 bar Formless**, so his own class data has always expected them. See `DECISIONS.md` 2026-09-21. Original entry follows. **Opened 2026-09-20** after Daryl found the faeries missing. Seven playable races have no `raceStatsAndMoveDetails` row — `applySingleRaceToAttribs` (sheet-worker.js:32987) assigns them inline in the dictionary's own 62-column shape — and the extractor only read the dictionary. `inline_race_stats` now walks that switch; **five ship** (Fairy, Fairy(Dark), Maginos, Podling, Sporeling), taking the ordinary branch and keeping the slight-physique rows beside them in `src/packs/named/inlineRaceStats.json`. Every other race table already had rows for all seven, including `getRaceHeightType`, so the five roll height and weight correctly. **Done when Famorian and Formless are in too:** neither is a literal row. Famorian adds 1d3 apiece to STR/AGL/VIT from its "evoke" checkboxes (33032–33624, and his `evokedict` is already extracted); Formless takes its whole physical half from a host race via `setFormlessStartingRace` (his `formlessStartingRaceDetails` is extracted too) and supplies only its own mental block. Both need runtime logic, and a row of zeros would give a character an attribute limit of 0 in all twelve rather than an honest gap. Two upstream answers are wanted first: items 38 and 39. |

## Epic 1 — Roll Engine & Data Model (Layer 0)

| Story | Status | Definition of Done |
|---|---|---|
| Define Foundry system manifest (`system.json`) targeting V14 | Done | JSON-validated; `compatibility.minimum: "14"`; folder skeleton (`module/`, `styles/`, `lang/`, `packs/`, `templates/`) in place with minimal stub files so the package structurally loads. **Not yet runtime-verified in an actual Foundry V14 install** — that's still outstanding. `documentTypes` declares `character`/`creature` Actor types backed by intentionally empty `TypeDataModel` stubs in `module/imagine-rpg.mjs`, placeholder only. |
| Design Actor data model (Character, Creature) | Done | Schema covers all fields cataloged from the Roll20 sheet's core tabs; documented against source page refs in `docs/reference/`. Source audit complete: field catalog extracted per tab, skills/equipment confirmed as Roll20 workaround inflation, Endurance confirmed derived + per-body-area. **Both schemas are built** — `module/data/actor-character.mjs` (826 lines) and `module/data/actor-creature.mjs`, the latter shipped with Epic 4 and answering the "parked on the Bestiary book" caveat this row used to carry. `docs/DATA-MODEL.md` was reconciled against the shipped code 2026-09-12 and now records that the code wins where the two disagree; **two drifts were corrected in the doc, not the code**: §9 step 2 still documented the Master's Manual attribute-max tiers (23/25/27/30) that Epic 2 had already removed in favour of `getAttributeMax(title, raceLimit)`, and the §1 source table still listed *Mysteries of the Planes* as a book not in hand. Of the doc's six open questions, four are resolved (sourcebook gating is content-only; creature schema is separate and leaner; saves use his half-chance tier while skills use the Player's Guide ±20% margin, which is the split question 4 guessed at; his flattened attribute tables are followed) and one is partly answered (three books still absent, 135 skills, prose only). **The sixth — "is `sheet-worker.js` current?" — remains open and is now far more load-bearing than when it was written**, since 4,367 documents and every combat rule derive from that file; it stays on the board above as blocking Epic 6. |
| Implement attribute-save resolver | Done | `rating × 5%` below 18, a 90% plateau for 18-20, then 90 + one per point above 20. **This is his `getAttribSave`, branch order intact** (`ImagineCharacterData.getAttribSave`); it happens to agree with the Master's Manual extended-range table. Unit-verified against both books' tables and the sample character screenshot, and re-run 2026-09-18 in the derivation suite. |
| Implement skill-check resolver | Done | `Base Chance + Ability Bonus` per Player's Guide p.94 formula; d100 roll-under; crit margin >20%; unit-verified with the book's own worked examples. |
| Design content/compendium layer (sourcebook tagging, enable/disable) | Done | `module/availability.mjs` resolver + four world settings + Game Master window (`module/apps/availability-config.mjs`, registered as a settings menu). Magic master switch and 16 subsystem switches as a hard ceiling, then `type:name` overrides (this is how a single class is disallowed), then sourcebooks. Enforced on `preCreateItem`: players blocked, the GM warned and allowed through. Items already on characters are flagged with a reason, never removed. **39 resolver and integration tests passing** (`tools/availability-test.html`); the 61 derivation tests still pass; all 13 modules parse; the window and the flagged sheet were checked visually. **Not verified:** the settings menu, the `preCreateItem` hook, the refresh on change and compendium index discovery all need a running Foundry V14. |

## Epic 2 — Character Foundation (Layer 1)

| Story | Status | Definition of Done |
|---|---|---|
| Attributes module | Done | **Closed 2026-09-18 with the language allowance.** `_prepareLanguages` derives `languageAllowance` from the Intelligence table using his `setLangSheet` rule. His two switches (the creation step at 49228 and the update step at 50561) agree for every rating 0-30. The derivation suite holds his labels for all 31 and checks `getLanguageSlotLabels` against each. The description tab now shows the actual picture, e.g. "5 of 4 in use, 2 of 2 written", with his slot label on each row. Slots are handed out by use (`assignLanguageSlots`): written languages take the writing slots first, per the book's "a written slot for a spoken slot, but not vice versa". Anything past the allowance is flagged, never removed. It follows current Intelligence, as his update step does. **Asked, not built** (`UPSTREAM-ISSUES.md` item 30): the book's Language Lore doubling and racial-slot sacrifice for languages, both absent from his code. Suites: derivation 275 (17 new); combat, creature and availability unchanged; 27 modules parse. Checked in the sheet preview against the real derived character. **Not verified:** anything needing a running Foundry V14. **Audit 2026-09-18, before this pass: everything except the language allowance was already built, and the row had not been updated** (the third time a Backlog row has turned out to be mostly done, after Skills and the five-class "gap"). Already done and tested (derivation suite, 258 passing): `_prepareAttributes` clamps each of the 12 to its maximum and carries the magical maximum beside it, then derives the save (his `getAttribSave`) and copies the whole matching row of his attribute table onto `attributes.<key>.mods`; `_prepareCharacteristics` derives Endurance, Perception, Affinity and Fortune with his `+.99` rounding, racial modifiers and title bonuses, plus Shock at Endurance × 3; `_prepareResistances` derives all five tracks, Control combining Will Force with the Intelligence and Wisdom adjustments, with immunity overriding. The Strength load limit is already feeding encumbrance (Equipment row). **The one real gap is the language allowance**, and the sheet currently misstates it: the description tab prints "Intelligence allows 0.25 spoken, 0 written", but his `setLangSheet(intelligence)` (sheet-worker.js:49228) shows that a value below 1 is partial fluency in the native tongue ("Speaks(quarter):" at Intelligence 4), not a quarter of a second language, and it builds up to ten labelled slots ("Speaks:" / "Speaks/writes:"), so writing lags speaking. The numbers in `ATTRIBUTE_TABLES.int` match his switch's case groupings for 0-21, as checked 2026-09-18, and 22-30 still need reading. **Next:** port the slot labels, show slots used against slots allowed, and flag rather than refuse an over-allowance list, which is the call already made for skill slots. The user chose Opus for this. Also outstanding, but not this row's: mundane/magical modifier channels for the magical ceiling (row below). Original DoD follows. All 12 attributes + derived stats (Endurance, Perception, Affinity, Fortune) + resistances, matching Roll20 sheet fields and Player's Guide tables. **Also owns the Intelligence language allowance** (assigned here 2026-09-12): `config-tables.mjs` already carries `spokenLanguages`/`writtenLanguages` as the Intelligence table's first two columns and nothing reads either. Derive them here with the rest of the Intelligence-derived values rather than on the description tab, which would scatter it. Values below 1 mean partial fluency in the native tongue, not a fraction of a second language, and writing lags speaking — read off his `lang_sheet` block names at sheet-worker.js:49257-49560. See `DECISIONS.md` → "The character sheet cannot show 19 fields the model carries". |
| Reconcile Character attribute maximums with his code | Done | **Superseded in part 2026-09-17 — the flat 27 described below is now 25, and there is a second ceiling.** His answer on `UPSTREAM-ISSUES.md` item 16: *"25 for normal statistic upgrades, 27 for magical upgrades."* `getAttributeMax` now returns 25 from title 11; the new `getMagicalAttributeMax` carries his `setMagicalAttributeMaximums` tiers (sheet-worker.js:123007): 23 at title 0, 25 at 1-10, 27 at 11+. The magical figure is derived and shown, but **nothing is clamped by it yet**. That needs modifiers split into mundane and magical channels as his `tmp_mod_str_mundane`/`tmp_mod_str_magic` are, and the port has one undifferentiated `permMod`/`tempMod` pair. That split is unscoped future work. See `DECISIONS.md` 2026-09-17. **Also found 2026-09-18:** the comment above `getAttributeMax` (`actor-character.mjs`) still says "flat 27" and "the Master's Manual tiers are NOT implemented". Both are now false, and the comment has been corrected. Original text: Fixed. `getAttributeCap(title)` is replaced by `getAttributeMax(title, raceLimit)`: the race's limit for that attribute, 20 before a race is chosen, and a flat 27 from title 11 — which is what his sheet does (race limits at sheet-worker.js:8099, `setArchMortalAttributesMax` at 27549, called at 66140). The Master's Manual's 23/25/27/30 tiers are gone, and `IMAGINE.attributeCaps` with them, because nothing in his code implements them: every `*_max` assignment was checked and there is no deity handler at title 16. This changes play in both directions — an arch-mortal of a limited race gains real headroom, and a low-title character of a permissive race is no longer held to 25 by a tier that does not exist. 91 derivation tests pass (`tools/derive-test.html`), covering the 20 default, race limits above and below 20, the title 10→11 boundary, and an arch-mortal whose race would previously have barred the rating; creature 121, combat 101 and availability 39 are unchanged, and all 25 modules parse. His own comment at the call site says 25 where the code sets 27 — logged as `UPSTREAM-ISSUES.md` item 16. See `DECISIONS.md`, "CORRECTION: a character's attribute maximum follows his sheet". |
| Races & Classes module (compendium-driven) | In Progress | **THE CLASS HALF IS DONE 2026-09-19; what is left is race-side and waiting on the developer.** Class skills are now granted automatically on reaching the title (`module/class-advancement.mjs`), which is his own rule -- his `getTitleToAcquireSkillsByGoal` makes the goal boundary and the title boundary the same line. A class skill held above the character's title is refused on the roll by title NAME, his `handleHighTitleClassSkillRoll`; that also closes the "cannot be used non-acquired" flag left open on the Skills row. A class's armour and weapon usage are shown beside the progression and never enforced, exactly as his sheet does. An availability override on a base class now covers its path documents, so forbidding `class:Elementalist` forbids all six. The progression panel was reading the wrong field and has been corrected to `classSkillList`, so every class shows its titles rather than the handful hand-authored. Derivation 347, availability 46; `DECISIONS.md` 2026-09-19. **Still open, all race-side:** Gaunt (question with him), the racial ability *mechanics*, and Multi/Part/Trace Race (`UPSTREAM-ISSUES.md` item 32). **Half races DONE 2026-09-18.** A character may hold two race items and becomes his Half Race, ported from `applyHalfRaceToAttribs` (sheet-worker.js:33770) into `module/race-rules.mjs`. His combining helper turns out to be the Player's Guide's own rule 1: a modifier only one race has is taken whole, bonuses averaged up, penalties averaged towards the bigger, a bonus against a penalty added. Special movement and body come from the first race. The name is his "first|second". His Multi, Part and Trace Race are listed but unimplemented in his sheet, so they are not built; a third race is reported. Asked in `UPSTREAM-ISSUES.md` item 32, along with the book's -5 Social Class and the breeding table. Derivation 304 (24 new). **The rest of a race DONE the same day:** racial skills with bonuses, abilities, disabilities, immunities, ages and fertile partners on every race, and the races each class is closed to, all generated from his tables. A half race merges them all, and an infertile pair or a barred class is flagged on the header. The Description tab has a Race panel. Derivation 320. **(d) below was wrong:** his `classRaceAndDetails` IS the race-to-class table (a barred-races list), and it is now used. **Classes finished the same day:** every class now carries its **per-title class skills from his `setClassSkillLists`** (92 classes, 4,360 lines; the board had said his data had none). A class with a choice is **one document per path**, with its own skills and alignment: Elemental Dancer x6, Elementalist/Summoner x2, Innominate x2, Inquisitor x2, Knight and Knight(Dark) x2. **GME** is built from his inline row as a non-classed class: a picked attack chart and no first-title Endurance bonus. 103 class documents. Derivation 325. **Left in this row:** Gaunt (question with him), the racial ability *mechanics*, and Multi/Part/Trace Race (item 32). The content-authoring method for every pack is the next pass. **Status corrected 2026-09-18; it was never Backlog in substance.** Built already: the race and class item schemas (`item-race.mjs`, `item-class.mjs`) with their own item sheets; 105 race and 88 class documents generated from his dictionaries, including Monk since the repair he authorised, plus hand-authored Elemental Dancer; racial attribute modifiers and limits, characteristic and resistance modifiers all read by the character model; class attack progression, the lore titles and skill-slot requirements; dual classing; movement (below). **Remaining, each a real item:** (a) **GME is not a class**. Per his 2026-09-17 answer it is a 0-title non-classed character that picks any social and racial skills one at a time and gets no racial title-1 bonuses, so it needs modelling, not a class document. (b) **Elementalist, Summoner and Inquisitor split on a good/evil choice** that changes their skills. He accepts either a choice field or one document per path; not yet decided or built. Both come from `UPSTREAM-ISSUES.md` items 22/26. (c) **Racial ability mechanics do not exist on the character**: his `setTempRacialAbilities` switch (infravision, hide values and so on) is unported; see `DECISIONS.md` 2026-09-18 on the dormant trait conflicts. (d) "Classes Available by Race" is book-only and was never extracted, so dual-class racial eligibility cannot be checked. (e) Gaunt's random stat (Epic 6 row). (f) The compendium round-trip in the DoD needs a running V14 (Epic 6 importer row). Original DoD follows. Race/class data shape defined; at least the sample race/class from source material round-trips through the compendium layer. **Also owns the special movement rate** (found 2026-09-12 from the user spotting zeros on the preview sheet). **The walk/jog/run half is DONE.** A race's movement figures are **modifiers on a base drawn from Agility**, not finished rates — his `calcMovement` switches on Agility and adds the race's figure — so every race carrying `0/0/0`, Human(Civilized) among them, was rendering as unable to move. `MOVEMENT_BASE` is now generated out of his switch (both copies of it, cross-checked) and applied in `_prepareMovement`; the preview character walks. A speed multiplier of 0 is his sentinel for x1, and a negative rate falls to the Player's Guide floor of 1 mile / 10 feet / 1 foot, which his sheet never implements. **The special rate is DONE too.** All 26 races that have one now resolve to real distances and none lands on zero. The five kinds do NOT share a formula -- only Scurry adds its additive, Slither alone ignores the speed multiplier and replaces ordinary movement outright -- and there are **three** base-rate names, Walk, Run and INT for magical flight. Spec in `docs/sonnet/2026-09-12-special-movement.md`; diagnosis in `DECISIONS.md`. **`UPSTREAM-ISSUES.md` item 23 — the claim that 14 races had no movement data — is WITHDRAWN; that was our misreading, not his gap.** New item 24 is real: Elf(Sea) and Elf(Ice) carry a -10 speed multiplier that his sheet multiplies through into negative distances. |
| Formless: inhabit and switch bodies | Pinned | **Pinned by the user 2026-09-22, to be revisited once everything else in the core is done.** A Formless can choose its host at creation (built 2026-09-21), but cannot keep a list of bodies or change which one it wears afterwards; his sheet does both. Until then a free-text `identity.formlessBodies` field, shown on the Description tab only for a Formless, is the record, and switching means replacing the host race item by hand. Done when a Formless can hold several bodies and pick the active one, at creation and in play, from his own code's model of it. |
| Race descriptions: clean for release | Pinned | **Must be done before public release; not before.** Eleven race descriptions (the eight split faerie forms, Maginos, Formless and Famorian) are the build's explanation of how the port handles the race -- "In his sheet wings belong to the slight-physique branch..." -- which is useful to a Game Master and the developer and wrong for a player choosing a race. Review each: move the port's reasoning to `DECISIONS.md` or a GM-only note, keep what a player needs, and raise anything that is really a feature request as its own row. Generated by `tools/extract/build_documents.py`, so fix it there, not in `races.json`. |
| Famorian: limit evokes to the chosen animal's package | Backlog | **Optional; the user's current call (2026-09-22) is that the Game Master polices it, as his sheet does.** Not in his sheet -- the animal type is free text there. The data exists in the bestiaries: about 190 animals carry an "Evoke Package:" line (104 in Aspects of the Wild, 87 in Legends of the Unknown). Done when the animal type is chosen from those animals (with a free-text escape for a GM-approved other), the evoke picker offers only that animal's package, an Inbreed may draw on more than one, and every prose trait is mapped to one of his evoke labels with the unmatched ones listed. |
| Skills module | In Progress | **What keeps this In Progress (re-read 2026-09-18):** all five build items below are closed, and what is left is data rather than code. There are two per-skill flags his `skilldict` does not carry, so both have to come from the books: `isRestricted` (`docs/sonnet/2026-09-16-skills-module.md` item 1), and, new since his 2026-09-17 answer on item 19, **"cannot be used non-acquired"**, meaning a skill held but unusable until its acquisition title. The port honours `isRestricted` already; nothing yet models the second flag. **Row corrected 2026-09-14: most of the stated DoD was already built and the row had not been updated.** The chance formula is done and tested — `_prepareSkills` (`data/actor-character.mjs`) implements Player's Guide p.94 (`base = (combined attributes - rating) x 5`, `total = base + starting + ability + misc`), with the combined-attribute rule (one attribute as it stands, two averaged and rounded up) in `_getCombinedAttributes`, and the derivation suite checks it against the book's own worked example. The common-skill fallback is done (base chance alone, no starting bonus, and it consumes no slot). Class/racial/social counting is done, and **the Knowledge slot allowance is done too** — `config-tables.mjs` carries his whole `knwRatingValues` table (class/racial/social slots plus memorization points, ratings 0-30) and `_prepareSkillSlots` reads it, with the skills tab showing used against maximum. A roll handler exists with the ±20% critical margin. **All five outstanding items are now closed (2026-09-16), and one of them turned out to be already done.** (1) **Duplicate skills** are rolled once per copy with the best taken, per the Player's Guide's "Duplicate Class and Racial Skills" and its worked Move Unseen example — the copies do not share a chance, so each is resolved against its own before they are compared, and every roll is shown on the card rather than only the winner. His sheet does not disagree: its one duplicate guard refuses the same skill twice *within* the racial list, which is compatible. (2) **Slot enforcement** flags rather than prevents — his sheet refuses the selection outright, but it refuses at a character-generation step this port has not built, and a skill here is an Item droppable from anywhere; flagging matches how availability already marks an item that should not be there. It needed its own derivation step, `_prepareSkillSlotStatus`, for the same ordering reason `_prepareOffhandSkills` has. (3) **The slot tricks are ported at his rates** from his four conversion functions (racial → 1 class or 2 social; 2 social → 1 racial or 1 class) plus both sacrifices (a slot given up for 2d4% on a skill already held, added to its modifier and not floored, as his code adds it). There is no fifth function — nothing converts out of class — so there is no such button. The moves are stored as six counts mirroring his own `tmp_*_slots_removed` attributes, and the allowance is derived from them, with the untraded figure kept beside it so the tab shows what a trade cost. One of his guards is deliberately not ported: social → racial also wants racial to have an unavailable slot to open into, which is his twenty-row sheet running out of rows, not a rule. (4) **Untrained rolls** take the base chance and nothing else, with the book's two limits applied by the caller — a skill already held is rolled as itself, and a restricted one cannot be tried. (5) **`getSlotsNeededForClass` was already extracted** and this row was stale: `class_skill_slots_needed()` has been in the generator since the class pass and all 87 class documents carry `skillSlotsNeeded`. **One real finding:** `isRestricted` has nothing behind it — his `skilldict` carries no restricted column at all (the seventh column is learn time: Climb "32", Second Weapon Lore ""), so all 674 skills read as unrestricted until it is extracted from the books, which is `docs/sonnet/2026-09-16-skills-module.md` item 1. The flag is honoured now so nothing needs rewiring when it lands. Suites: derivation 227 (44 new), combat 345, creature 139, availability 39 unchanged; 27 modules parse, the new `module/skills-rules.mjs` among them. Sheet preview exercises a real trade (one racial for two social: "RACIAL 1 / 13 was 14, SOCIAL 1 / 14 was 12") and the isolated render doubles as the refused-state check. **Not verified:** anything needing a running Foundry V14 — the two dialogs, the 2d4 landing on the chosen skill, and the untrained picker, which reads the skill compendium and cannot be exercised in a preview at all. Full account in `DECISIONS.md`. |
| Dual Class Characters | Done | **Built 2026-09-16, from the Player's Guide alone — his Roll20 sheet carries no provision for this at all**, one `classname` field and nothing else. A character may hold more than one class item; `classItems` is the full list and `classItem` stays the first, so nothing reading one class had to change. **Each class advances on its own title** — the book's own worked example is a Mage/Warrior who "advances from 1st to 2nd Title in the Mage class only" — so title moved onto the class item itself (`system.title`, 0 meaning "follow the character's"), with per-class stepper buttons on the header once a second class is present. The attack chart is the greater of the two classes' charts, each read at its own title (Experience and Advancement rule 5); a lore or the Lore chart itself is held if EITHER class has reached it, again each at its own title, read as the "better applies" principle of rule 4 since the book does not rule on lores directly. Class skill slot requirements are summed across both classes, which is the book's own stated reason the slot tricks exist ("a dual classed character will need to have many class skill slots"). Requirements (Knowledge 15, both classes' attribute minimums) are **reported, not enforced** — dual classing needs the Game Master's support per the book, so the sheet lists what falls short rather than refusing it. **The racial half of the requirement cannot be checked**: his data has no race-to-class permission table at all ("Classes Available by Race" is book-only and was never extracted), so that half is left to the Game Master and said so rather than silently skipped. Suites: derivation 251 (24 new), combat 352 (7 new, `getBetterAttackSkill`), creature 139 and availability 39 unchanged; 27 modules parse. Sheet preview gained a real second class (Mage, title 4, beside the Warrior at 12) and shows "Warrior/Mage" with both per-class titles. **Not verified:** anything needing a running Foundry V14 — the title steppers writing back and the header re-rendering. |

## Epic 3 — Assembly & Play (Layer 2)

| Story | Status | Definition of Done |
|---|---|---|
| Character Generation module | In Progress | **Height, frame, weight and the colour tables landed 2026-09-19**, which were the gaps a table would meet first: `tools/extract/extract_physique_tables.py` walks his twenty-odd height/frame/weight functions into `module/physique-tables.mjs`, `module/physique-rules.mjs` rolls them in his own order (height, then frame from STR less AGL, then the weight that needs both), and the Details step has one Roll button for all three plus selects for hair, eyes and skin off the race's own lists. `UPSTREAM-ISSUES.md` item 37 was found doing it: four races get no height on his sheet at all. **Built 2026-09-19 as a step-by-step window** (Create Character button in the Actors directory, or `game.imagine.generateCharacter()`), following his own nine creation steps in seven: Basics, Race (one, or a half race with only fertile partners offered), Attributes, Class, Skills, Details, Review. **Attributes:** his per-attribute dice (STR/AGL/VIT/AUR 7d4 keep 5; INT/WIS/KNW/APP/CHM/PTY 6d4 keep 5; SOC/WIL 5d4), with Adventurer, Heroic and Legendary rolling 1, 2 or 3 sets and keeping the best; the book's Normal 5d4 at 2:1; 3:1 swaps with Social Class never moved and nothing below 5; the Civilized Human's three points and six moves (points only for a half race); slight physique. **Class:** qualification against the final attributes and barred races, with his override. **Skills:** the title-1 class skills, with the no-casting alternative for a race that cannot cast; racial and social picks up to Knowledge's slots. Starting bonuses are his: class dice +30 if core + matching type modifiers, racial dice doubled + the race's bonus. **Details:** handedness by his d100 bands, age in the race's range, looks, alignment, languages against Intelligence, money. It creates the actor with its race, class and skill items. Rules and view are pure, so the new `tools/chargen-test.html` (46) tests the rules and `tools/chargen-preview.html` renders every step through the window's own view builder with real content. The created character derives exactly the attributes the generator showed. **Remaining:** height, frame and weight from his tables, colour tables, starting money and equipment, social-class skill modifiers, and the special races (Changeling, Formless, Famorian). See `docs/sonnet/2026-09-19-character-generator.md`. **Not verified:** the window in a running Foundry V14. Original DoD follows: Normal/Adventurer/Heroic/Legendary rolling methods implemented per Player's Guide p.20; attribute point-buy (2:1 or 3:1 per method) implemented. |
| Experience & Levelling module | In Progress | **Built 2026-09-19.** His whole ladder, generated rather than transcribed (`tools/extract/extract_advancement_tables.py` reads four of his functions and cross-checks them, which is how `UPSTREAM-ISSUES.md` 35 was found). `module/advancement-rules.mjs` holds the rules, `module/advancement.mjs` writes to the actor, `module/levelup-view.mjs` says what the window shows and `module/apps/level-up.mjs` is the window -- a Level Up button on the sheet header. Experience is queued as his titles_to_raise/goals_to_raise; his three refusals are kept, including the goal-30 Arch Mortal gate. A goal rolls his attribute offer (5/10/20% by shape and goal) with his guaranteed pick at goals 12, 27 and 42, and spends the class's 15-21 skill points at +1% each. A title brings its class skills, its Endurance (max rather than a roll from 11th, x2/x3/x4 at 13/14/15) and, from 11th, the full Arch Mortal package: maximums to 27, ageing stopped, Sense Supernatural by title, and the invulnerability as a real power item that replaces the one before. The qualification screen is derived from his archmortalqualifylist, now on every class document. Advancement 77 tests; `tools/levelup-preview.html` walks all three steps with a real Warrior. **Left:** the magic side of a title (Aura/Piety control, regeneration, Spell Lore) waits on Layer 4; experience AWARDS (his calcCreatureExp and party split) are deliberately not built -- the user's call, players enter what they were given. **Not verified:** the window in a running Foundry V14. |
| Combat module — phase 1 | Done | Attack charts (7 levels, from his code), with attack skill worked out from class progression and title. d20 attack down his exact zone ladder, with fumbles (Agility save, then the 80/20 split), called shots judged on the natural roll, and the target's defensive adjustment. Damage uses his Strength rule (a bonus doubled two-handed, a penalty halved), plus magic bonus, called-shot half damage and the ×3 cap. 45 body charts give per-area Endurance, and armour is summed per area from worn layers. His banded blocking, degradation and hide are in, with wound thresholds for the Vitality save, area effect and shock. The 10-second round is an ascending-initiative tracker in which actions spend seconds. Combat tab on the sheet, attack chat card, Apply Damage and Spend Seconds. **101 combat-rule tests** (`tools/combat-test.html`), including the Player's Guide's Brom called-shot example and a check that all 86 classes' attack progressions parse; **26 new character-integration tests** (87 in the derivation suite); all 17 modules parse. The sheet and chat card were checked against a rebuild of his own sample character from real content. Six bugs in his combat code and three rule/code disagreements are recorded in `UPSTREAM-ISSUES.md` items 6-7. **Not verified:** everything that needs a running Foundry V14 — the dialogs, the chat card buttons, the tracker's sort and round handling, and actor updates. |
| Combat module — phase 2 | In Progress | **Per-body-type armour mapping is done** — `ARMOR_COVERAGE_BY_BODY_TYPE` and `ARMOR_REQUIRES_ITEM` are generated from his `getArmorValuesByBodyTypeAndArmor` and read through `getAreaArmorSlot`/`getAreaArmor`, so non-humanoids are no longer unprotected by worn armour and barding works. Keyed by area name rather than his positions, because two of his branches have drifted out of step with their charts (`UPSTREAM-ISSUES.md` items 17 and 18); the generator reports every disagreement. Regeneration added 217 lines and removed none, and all four suites pass unchanged. **The critical fumble table is done** — `resolveCriticalFumble` ports his `getCriticalFumble` d100 ladder (nine damage bands, tripping, and at 99 and 100 losing the weapon with an armour-bypassing variant), returning the consequence as data — target, damage multiplier, whether it bypasses armour, whether the weapon is lost or broken — as well as the sentence his sheet printed, so a fumble's damage can actually be applied. 147 combat tests pass, 23 of them new. **The Lore attack chart is done** — a class carries `loreAttackTitle`, generated from his 92-case `getLoreAttackChart` switch, and the character derives `combat.loreAttackSkill` (the standard chart one level up) once that title is reached; 39 of 86 classes reach it, and the combat tab shows it. His class rows carry a second, different number for this in `attackSkillList` ("Grandmaster(mastered weapons at N)") which disagrees with the switch for all 38 classes carrying both, so only the switch is used — see `DECISIONS.md`. **Shield coverage by handedness is done** — `SHIELD_COVERAGE` is generated from his `equipShield` (family, size, handedness) and read through `getAreaShield`, which both actor models add as a fifth layer on top of the worn armour. A shield is held in the off hand, so a right-hander is covered down the left side, and the sizes grow outward from the hand: Buckler the forearm or the hand, Small both, Medium the arm, Large the shoulder, Body the whole flank. His index convention was verified by hand against every chart before anything was written — it holds for six families and fails for Snake and Centaur in exactly the way his armour branches do, so `UPSTREAM-ISSUES.md` items 17 and 18 now record a second occurrence in a second function. This also removed a double count: a shield's value lives in the left-hand coverage column whichever hand holds it, so treating it as ordinary armour had been protecting every wearer's left hand and nothing else. Ambidextrous reads as right-handed, following his else branch; the buckler's wrist option and a `magicBonus` are new fields on armour, and handedness is now a field and a dropdown on both actor types. **A bug in our own port was fixed on the way** — keying armour coverage by area name had silently left 22 area instances across eight charts with no worn armour at all, because his case comments and his charts do not always spell an area the same way (`Left Hoof` against `Left Foot`, `Left Lower Leg` against `Left Shin`). One resolver, `chart_area_lookup`, now serves both tables and matches exact, then normalised, then positional while the two are still in step; 106 chart-area instances had no armour slot before and 83 do now, the remainder being areas his branches genuinely leave bare. Suites: combat 183 (36 new), derivation 116 (17 new), creature 129 (8 new), availability 39 unchanged, 26 modules parse; both sheets checked in the preview harnesses against a real `Shield(Large/Plate)`. **Not verified:** everything needing a running Foundry V14 — the new fields reaching the database, the dropdown writing back, and a re-render when a shield is equipped. **Pain threshold is done** — `applyPainThreshold` ports the first thing his `handleBodyDamage` does to a blow, and every Apply Damage runs through it before armour. It is a SIGNED modifier on incoming damage, not a level to clear: his own note beside the field reads "reduces or adds to all incoming damage (-/+)", so a negative number is the tougher target. Both actor types carry `combat.painThreshold` and a `combat.highPainThreshold` flag for the Famorian evoke's extra point, which nothing sets until evoke mutations are ported. **Damage absorption is done** — `absorbDamage` ports the last thing his handler does to a blow, after armour and hide. It is a POOL, not a per-blow reduction: it takes what it can and is spent by the same amount, so it wears out, and applying damage now writes the reduced pool back. Where his refills it from (a `Rune Absorption: +N` on worn armour, or the Game Master's modifier, whichever is larger) belongs with runes and magic armours, both still to do, so for now it is entered by hand. `blowLands` was added alongside it, porting the guard that decides whether a blow does anything at all — **this corrected a mistake in the pain-threshold pass**, which had read that guard as covering only the armour blocking when it in fact wraps the armour damage, the hide reduction, the absorption and the store as well, so a hit that rolled under 1 does nothing whatever. **Magic armours are done** — `applyMagicalReductions` ports his whole pre-reduction block (invulnerability, spiritual, force and outer kinetic armour, magic shield and magical weaves), which completes the damage pipeline: pain threshold, magical protection, armour blocking, armour wear, hide, absorption, wounds. Invulnerability is the only one that scales, by the weapon's magical plus; the rest subtract, and the floor lands once at the end. **An endured or rebounded blow does nothing at all** — his handler branches past the whole apply block, so it is total immunity rather than a reduction. `ENDURED_BY` is generated from his switch and caught that "Enduring All" covers nine damage types and not Obliteration (`UPSTREAM-ISSUES.md` item 20). A magical weave is counted as hide and then taken back out of the armour total, as his code does, so it is never counted twice. The automatic sources for these values (base aura, base piety, runes) belong to the deferred magic subsystems, so the fields are entered by hand for now, with the rule that they take a maximum rather than stacking recorded at the schema. **Per-weapon lore is done** — a class carries `weaponLoreTitle` and `missileLoreTitle`, generated from `getWeaponLoreWhen`/`getMissileLoreWhen`, the character derives whether it holds each, and one comma-separated list per kind names the weapons singled out for the larger bonus. General lore is attack +2, damage +4, speed -1, skills +10%; a specifically lored weapon gets +3/+6/-2/+20% INSTEAD, not on top — his own comment on the speed says so. **These were on the blocked list and should not have been:** item 19 blocks the lore tables because the `=>` gates would have to be guessed, but a later planning pass found that **all six** are gated correctly inside `setGeneralCombatModifierDisplay`, and that none of the seventeen broken gates is in that function — so none of the six is blocked on that question. Item 19 now carries the corrected table. That pass also found that his numeric path never applies the per-weapon tier at all (only the general +2/+4/+10% reaches `combat_mod_*`), and that the general -1 weapon speed was deliberately removed; the port keeps both tiers and -1/-2, implementing what his own modifier panel promises, which is logged as a deliberate departure in `UPSTREAM-ISSUES.md` item 21. **Projectile Lore is done** — the odd one of the family: worth damage PER DIE (`getNumberOfDice(damage)` x1 general, x2 for a listed projectile) rather than a flat figure, so flat-damage ammunition gets nothing, and it attaches to the AMMUNITION rather than the weapon in hand, so a bow is lored through the arrow it fires. Six of the 92 classes acquire it, all archers. His three name chains are generated as ORDERED arrays because the order is load-bearing — "Bolted" is tested before "Bolt" so a bolted-leather shield does not read as a crossbow bolt. This pass also **corrected item 21 a second time**: the attack path DOES apply the per-weapon tier, by subtracting the general modifier and applying the specific one, which confirms the port's replace-don't-stack design from his code rather than from a comment; the real defect is narrower — `MLDamMod` is missing from the damage sum, so a lored missile weapon never rolls its +6. 291 combat tests pass, 108 new across the five items; 123 derivation tests. Still to do: the four remaining lore types — none blocked, but they are not one task: Second Weapon Knowledge and Second Weapon Lore are the top of an **off-hand fighting subsystem** (off-hand penalties by handedness and ambidexterity, with the skill's own percentage buying levels of penalty removal, ~324 references) and want their own planning pass;  Multiple Missile Lore belongs with the multi-missile item below. Also martial arts and stances, multi-missile, soldiering, runes, special damage effects, carry-over, and evoke mutations. The magical pre-reductions (invulnerability, spirit/force/outer kinetic armour, magic shield, weaves) all sit in the same block as pain threshold and their order is now recorded at the call site, so they slot in without re-deriving it. **Off-hand fighting, first half, is done** — the base penalty. The user settled both design questions, and the second answer improved the model: a weapon is tagged `hand` = right/left/both on the combat page, so **off-handedness is DERIVED** against the `handedness` field the shield pass already added, rather than stored. That one three-state field **replaced two booleans that could contradict each other** (`twoHanded` and `offhand`, both of which turn out never to have been populated from his data, and `offhand` was dead — declared and never read); the Strength damage rule now reads `hand == "both"` from a single source. An Ambidextrous wielder has no off hand at all, matching the short-circuit at the head of all three of his penalty functions — ambidexterity is the absence of the cost, not a bonus. The three Agility-banded tables are **generated** by a new `banded_chain()` into `OFFHAND_PENALTIES`, with a contiguity check over 1..30 that came back clean for all three; **their band edges genuinely differ** (melee zeroes at Agility 19, damage and skills at 20; only damage breaks out 16 and 17), which is why generating rather than transcribing was the right call. **The finding worth carrying: `getOffhandMeleeAdj` is not melee-only despite its name** — his `offHandPenalty` is in both `totalmods` branches (sheet-worker.js:64804 missile, 64806 melee), so gating on melee would have quietly buffed every off-handed archer. **Deliberately stubbed:** the "knowledge" tier resolves and is reported but buys nothing down yet, since `skillChance / 20` needs skills resolving to a number on the actor — that is the second half, along with Second Weapon Lore's seconds. Until then Knowledge alone pays full, which is his own behaviour below one level. **The tag is on the combat page**, as a Hand column of three buttons (L / R / 2H) with a derived "off" marker beside a weapon in the wrong hand — buttons rather than a dropdown because an ApplicationV2 `data-action` is dispatched from a click, and a `<select>`'s change binding cannot be verified without a running V14. **A gap in the preview harness was closed on the way**: `tools/sheet-preview.html` duplicates the sheet's weapon-row builder rather than calling it (it is private), so the new fields rendered blank there while being correct in the sheet — the harness had silently stopped exercising what it appears to. Both fields were added to the duplicate, with a comment that it must be kept in step, and the preview's dagger is now tagged left-handed so the off-hand path is genuinely exercised. **291 → 330 combat tests, 39 new**; derivation 123, creature 129, availability 39 unchanged; 26 modules parse, 0 syntax errors. Combat tab checked in the preview against a real derived character. **Not verified:** that clicking a hand button writes back to the embedded item and re-renders, and that the modifier reaches the chat card — both need a running Foundry V14. **Off-hand fighting, second half, is mostly done.** The two remaining lore-title tables (`get2ndWeaponKnowWhen`, `get2ndWeaponLoreWhen`) generated cleanly beside the existing five, giving `secondWeaponKnowTitle` / `secondWeaponLoreTitle` on the class item and `hasSecondWeaponKnowledge` / `hasSecondWeaponLore` on the character, exactly as the other four lores derive. **The Knowledge buy-down is implemented**: `floor(chance/20)` levels off the actor's own "Second Weapon Knowledge" skill, each buying back one point of melee and damage penalty and 5% of skill penalty from the *same* banded tables the full-penalty tier reads, floored at zero independently on each of the three figures rather than only when all three would cross at once. A new derivation step, `_prepareOffhandSkills()`, runs immediately after `_prepareSkills()` because `_prepareCombat` — where eligibility is derived — runs *before* skills finish computing their `totalChance`, so the skill lookup could not live there without reordering established derivation. **Second Weapon Lore's seconds discount is genuinely blocked and is not built**: every reference to `offhand_2nd_lore_seconds` was checked, and his sheet computes it, stores it, and reads it back exactly once — into a display string. It is never subtracted from anything a round or an attack costs. The RESOLVED design answer gives the *direction* (fighting off-handed costs seconds; Lore buys them back) but not the *number* — what an un-bought-down off-hand attack costs before five levels reduce it to nothing — and his sheet never picked one. Put to the user rather than invented. **Creatures confirmed to use none of this**: `handleCreatureAttack` has no reference to off-hand, second weapon, or hand at all, which follows from his creature attacks being named slots rather than weapons assigned to a hand — the creature model needs no `hand` field. 330 → 335 combat tests, 5 new; 173 derivation tests, 11 new; creature 129 and availability 39 unchanged; 26 modules parse. **Not verified:** anything needing a running Foundry V14. **The chat card now shows lore's damage contribution** (`damage.lore`/`damage.loreSpecific`/`damage.projectileLore`) — the to-hit line needed no new code, since it already iterated a generic mods list. Confirmed a `speedSpecial` weapon's speed (a Garrote, "no ordinary swing timing") stays "Special" even when specifically lored, via the sheet preview, which also turned up and fixed a real drift: the preview's own duplicate of the private `#buildWeaponRows` had silently dropped the lore speed term for every weapon it rendered. **A genuine gap found, not yet fixed:** his sheet applies a real numeric Weapon/Missile Lore bonus to CREATURE attacks too (`setCombatModifierValues` branches on `getCreatureSkillChance`, feeding `combat_mod_melee_other`/`combat_mod_missile_other`, which `handleCreatureAttack` reads directly) — the port's `creature-attack.mjs` has no equivalent at all. Left for a design pass, since a creature has no per-weapon lore list the way a character does. Full account in `DECISIONS.md`. **Projectile Lore's Lore panel and chat-card line are both confirmed** — a real Archer at title 12 (weapon/missile/projectile titles 12/3/7, all reachable together) shows all three rows on the panel and "+10 projectile lore (2/die)" on a real Long Bow's chat card, both as a second isolated render on the sheet preview. **One cross-check turned out to have no data to run at all:** whether the six Projectile-Lore classes match `skilldict`'s own class associations — but `skilldict` carries no class associations for any skill, only a flat catalogue with category/attribute/rating, so the comparison the note proposed cannot be made with data that exists today. 335/180/129/39 across the four suites, 26 modules parse. **Off-hand fighting's last two blockers are both answered and built.** The developer confirmed the seconds figure his sheet never computed: 5 of the round's 10 seconds by default for an off-hand action, one more per 20% of Second Weapon Lore up to 5 extra, 10 outright for Ambidextrous — matching the Player's Guide's own "Timing in the Combat Round" (p.178) almost word for word. `getOffhandSecondsCap` computes it and the Combat tab shows it next to Weapon Speed, exposed as a cap rather than enforced as a round-tracker pool (that would need new per-combatant, per-round state `combat-document.mjs` does not have — left open for the user rather than decided alone). **Creatures now take off-hand penalties too** — a rule the developer added on top of his sheet rather than one hiding in it; `handleCreatureAttack` genuinely never referenced off-hand at all, confirmed correctly by the earlier pass. `item-creature-attack.mjs` gains a `hand` field (blank means "not hand-based," never off-hand — a bite or a breath, unlike a weapon which always has a hand), `actor-creature.mjs` derives an effective handedness from the Ambidextrous/Fully Ambidextrous/Omnidextrous abilities (all three read as "Ambidextrous," and the ability wins over a stored handedness rather than deferring to it), and `getCreatureToHitModifiers`/`getCreatureDamageMods` gained the same `offhand` parameter the character functions already had. Built entirely by extending the already-tested character pattern — `resolveOffhandPenalties`, `isOffhandWeapon` and `getOffhandPenalty` needed no changes at all. 345/184/139/39 across the four suites (24 new), 26 modules parse; both the character and creature sheet previews, and the item preview's new Hand field, checked against real data. **Not verified:** anything needing a running Foundry V14. **Multiple missile fire is done, which closes the lore family.** Firing two or three projectiles, or two weapons at once, at the Player's Guide's own penalties (-4/-6, -8/-12, -4 to hit) -- which his three situational checkboxes carry identically, so nothing had to be chosen between book and sheet. Both multi-missile skills are learned ONE launcher/missile combination at a time rather than naming weapons for a larger bonus the way the other three lores do, so the character carries two new combination lists in his "Launcher/Missile" shape and a combination matches whether the weapon in hand is the launcher or the ammunition. Lore removes the penalty entirely, Knowledge buys it down 1 to hit and 2 damage per 25% of the skill (the only buy-down in the system that is not 20%, in both his code and the Master's Manual), capped so it can cancel but never become a bonus. **Two findings filed:** item 28, his sheet resolves double/triple fire as ONE roll whose damage repeats where the book says to roll each attack separately -- the port follows the sheet per the standing rule, and `resolveMultiMissile` reports `shots` so switching is a small change if he wants the book's version; and item 29, the Knowledge skill's own worked example ("at 50%... no penalties") contradicts its own rule, which leaves -2/-2 at 50% and only clears at 100% -- his code implements the rule and so does this. **Deliberately not built:** the third thing both skills cover, penalties for special projectile types, which is his switchedProjectiles subsystem and has nothing to attach to until special projectiles land; and the "point blank or short range only" restriction, a dialog hint rather than a gate since the attack flow does not model range. Suites: combat 374 (22 new), derivation 251, creature 139, availability 39; 27 modules parse. The preview's real Archer at title 12 shows both new Lore rows and fires two arrows end to end -- "Multiple Missiles -2" to hit, "-2 multiple missiles" in the damage breakdown, "23 each x 2 projectiles" for 46. **Not verified:** the new Firing dropdown and the two list fields writing back, both needing a running V14. |
| Situation Mods (melee and missile situational modifiers window) | Done | **Built 2026-09-22 at the user's request** ("a ranged and melee modifiers popup/popout submenu"). His SITUATION MODS bar and its two panels, as a window opened from a bar on both Combat tabs (`module/apps/situational-mods.mjs`, view in `module/situational-view.mjs`, rules under `@MARKER SITUATIONAL MODIFIERS` in `combat-rules.mjs`). Every figure from `handleMeleeSet`/`handleMissileSet`, not his labels; additive multipliers held at x3; his one-of groups; six options set only by a skill roll with Crit/Crit Fail. Stored on the actor (`combat.situation`) because it outlives an attack and its defence is the character's own -- `defensiveAdjust` moves with it and `noDefense` is read off the target. Weapon and creature attacks both read it: to hit, flat and per-die damage, maximum damage, the multiplier, half damage, No Attack; the rest are printed as notes. Three label/code disagreements and one missing `getAttrs` filed as `UPSTREAM-ISSUES.md` 53-55. Martial blind fighting and stance plug in through `combat.martialBlind`. Tests: combat +47, derivation +4, creature +4; `tools/situation-preview.html` renders the real template. **Not verified:** anything needing a running Foundry V14 (the window's re-render on actor update, `evaluate({ maximize })`, the dropdown listeners). |
| Second Weapon Knowledge/Lore held per weapon | Done | **Fixed 2026-09-22 -- they never applied in play.** The off-hand tier read two weapon booleans nothing ever set, so every off-hand attack paid the full penalty. Now as his sheet does it: the character names weapons in `secondWeaponKnowList`/`secondWeaponLoreList` (his `second_know_list`/`second_lore_list`), `getSecondWeaponFlags` derives each weapon's tier from them behind the title gate, and slots are one per title held (`getSecondWeaponSlots`). The booleans are removed from the weapon. The Combat tab's Lore panel carries both lists with their slots and Knowledge levels, the weapon row's off-hand tag names the tier with its figures, and the chat card now shows the off-hand damage it was already rolling. Tests: combat +14 (including the end-to-end case that failed before), derivation +4. |
| Martial arts: Martial Knowledge, Martial Lore and stances | Done | **The user's four rulings, built 2026-09-22:** Spinning +2 (the book); Martial Lore values take the book's time (Flip 2 s, Feather Block +1, Slam +2); a held stance's skill, save and resistance bonuses apply, with an intoxication count and a "resisting a hold" tick for the two conditional ones; Martial Lore blind fighting is the book's +2 hit / +1 damage / +5% combat skills per 25%, melee and missile, capped at the penalty; and creatures have the whole panel, sharing one derivation (`deriveMartialArts`) and one template partial. See DECISIONS, the two 2026-09-22 martial entries. **Built 2026-09-22 at the user's request, from his code** (every function reading his eight martial dictionaries, sheet-worker.js:66168-69100 and 98801-100838), the Player's Guide pp.93/95-97 and Mysteries of the Planes p.167 for the stances. **Generated:** column maps for the eight dictionaries (`column_maps.py`, 37 clean) and `extract_combat_tables.py` emits them into `combat-tables.mjs` with the numbers his dictionaries do not hold, walked out of `handleStanceOn`, `handleMartialModifierSet` and `handleMartialLoreModifierSet`, plus the four preset disciplines and Lore's blind fighting; every subskill modifier is checked against the p.93 rule each run. **Rules** in `module/combat/martial-arts.mjs`: subskill chances (the book's own Kick example is a test), what is known from discipline plus Lore-learned, stances learned and mastered, moves made with his Spinning rule and the Martial Lore override, Flip, blind fighting, a martial attack's damage (half on a failed skill roll), touch, failed moves, and the four learning rolls. Where his code argues with his own prose and the book sides with the prose, the prose is followed through three small correction tables (UPSTREAM item 56, which also carries seven outright slips, the worst being that a made move never counted on his sheet). **Character:** `system.martial` in his list shape, derived by `_prepareMartialArts`; a stance's defence, initiative and speed are folded into the combat figures everything already reads; per-attack effects come from `getMartialAttackModifiers(state, { mode, martialAttack })` in `getToHitModifiers`' `{ label, value }` shape; blind fighting goes to the Situation Mods as `combat.martialBlind`; Immoveable Stance's no-defence as `combat.martialDefense`. **Combat tab:** a Martial Arts heading that opens into the discipline and stance dropdowns, move chips, every subskill with its roll, Lore values and the learning rolls (`module/martial-view.mjs`, shared with `tools/martial-preview.html`); martial attacks post a card on the weapon card's own flag, so Apply Damage is the one pipeline. Suites: martial 150 (new), combat/derivation/creature/availability unchanged; the real Combat tab and card rendered against a real Martial Artist at title 6. **Not built:** missing-limb checks (no lost limbs in the port), stance bonuses to other skills and saves (his sheet prints them only), Lore value speeds (not in his table), creature martial arts UI, and wiring `getMartialAttackModifiers` into the weapon attack path -- `docs/sonnet/2026-09-22-martial-arts.md`. **Not verified:** anything needing a running Foundry V14. See `DECISIONS.md` 2026-09-22. |
| Round clock (Mr. Initiative): each combatant's seconds through the round | Done | **Built 2026-09-22 at the user's request, from his Master's Manual chart (pp.98-100) and the Player's Guide (pp.168, 174, 178).** Rules in `module/combat/round-rules.mjs` (pure): places in the round (ten, with extra seconds splitting the first ones into a/b), start from the roll, spends on the main hand and the off hand, seconds lost, the off-hand pool and its ceiling, the split second, the sort key, and carry-over of an action (reaction roll on finishing, 0 or less counts as 1) or of a late start (no roll). `combat-document.mjs` keeps the clock in combatant flags, spends/undoes/carries, sorts by it and starts the next round from the carries; `ImagineCombatant._preUpdate` restarts a clock on any outside initiative. `combat/combat-tracker.mjs` (`CONFIG.ui.combat`) draws the clock under each tracker row; `apps/round-clock.mjs` is the Mr. Initiative window; `round-view.mjs` builds both; one shared partial, `templates/combat/clock-bar.hbs`. New actor field `combat.speedSeconds` (both types; a box on each Combat tab); creatures now derive `offhandSecondsCap`. Weapon and creature attack cards record `hand` and spend once. Tests: round 87 (new), all other suites unchanged. **Not built:** his chart's Surprise row; effects that set speed seconds; see `docs/sonnet/2026-09-22-round-clock.md`. **Not verified:** anything in a running Foundry V14. UPSTREAM 61. |
| Weapon mods window: temporary effects (Bless) and his Customize panel | Done | **Built 2026-09-22 at the user's request.** A wand on each weapon row (Combat and Equipment tabs) opens `module/apps/weapon-mods.mjs` over `module/weapon-mods-view.mjs`; rules `module/weapon-custom-rules.mjs`, data `module/weapon-custom-tables.mjs` generated by `tools/extract/extract_weapon_custom_tables.py`. Temporary effects on `system.tempEffects` by world time: Bless (PG p.118, +1/+1 good, -1/-1 evil, a day) and the Game Master's own. His panel's weapon half on `system.custom` and `system.condition`, with his rules (plus clears quality/condition, Blessed or +N, base Aura/PC once set, `updateMagicPlus`, `checkWeaponCustomization`), applied to the weapon or a customized copy split off the stack. The name is never changed (the lore lists match by it). `rollWeaponAttack` adds runes (to hit, +Nd6+N), Blessed and temporary figures, a mode ability's dice and its natural-roll double; the card lists Foe Strike, energy, Bane and the divine abilities for the table. **His listing changes** (`setEquippedWeaponInCombatSheet`: what each customization, quality, condition and plus does to damage, speed, minimum, reload, length, skills and strength) generated as `WEAPON_LISTING_CHANGES`/`MAGIC_PLUS_LISTING` and applied by `getCustomizedWeapon` for the attack and the row, nothing stored. Tests: `tools/weapon-mods-test.html` (69). UPSTREAM 63-65. **Not verified:** the window in a running V14. |
| Visual attack-chart "bullseye", correlated to the target's body form | Backlog | **User's idea, 2026-09-14, confirmed against a screenshot of his own Combat tab; not yet designed or started.** His sheet renders the current attack chart as a literal dartboard: a Center bullseye ringed by Left/Right/High/Low hit wedges, then a Miss ring, with the natural/modified roll bands that produce each ("9-11", "19+", etc.) printed inside their wedge, plus corner call-outs for the Called Shot threshold and (Beginner only) the Miss(Short) band — one full diagram for the main hand, a second beside it for the off hand (its wedges read "-" with nothing equipped). This is a pure visualization of numbers already ported: `ATTACK_CHARTS` in `combat-tables.mjs` and the zone ladder `resolveAttack` walks in `combat-rules.mjs` (`Hit(Center|Right|High|Left|Low)`, `Miss(...)`, `Miss(Short)`, the called-shot and fumble thresholds) are the same bands this diagram draws as wedges — no new numbers, just a new way to show ones that exist. Beside it, his sheet also has a Combat Modifiers grid (Damage/Defensive/Melee/Missile/Initiative/Weapon Speed/Skills, broken out by STR/AGL/INT/Weight/Armor/Other into a Total and an Off Hand column) and a Roll Initiative button, none of which the port currently lays out this way even though the underlying figures (`getToHitModifiers`, `getWeaponSpeed`, `getInitiativeModifier`, etc.) all exist. **The user's addition on top of the diagram itself: correlate a wedge to the actual target.** Landing in "Hit(Left)" should not just print a word — it should indicate which of the TARGET's actual body areas that means, which depends on the target's own body form (a Humanoid's left differs from a Centaur's or a Snake's), using the per-body-type chart and per-area Endurance/wounds/armour already generated for both actor types (`getBodyChart`, `body.areas`) and the called-shot flow that already names a specific area explicitly. Needs a design pass before any code: how a directional wedge (`Left`/`Right`/`High`/`Low`/`Center`) maps onto a body-chart area for each body type (his zone names are directional, not body-part names, and there is no existing table doing this translation), what the diagram itself looks like in Foundry (redesigned for its own conventions per this project's stated direction, not a pixel clone of his Roll20 layout), and whether `body.areas`' existing wound tracking already covers "damage accumulates on the target across a fight" or wants its own visual overlay. **Refined 2026-09-16, from a mockup walkthrough:** the wedge-to-area mapping is NOT a fixed per-body-type table after all — the user corrected this against two examples (aiming at a Humanoid's neck; "a left to right slash"). A directional zone is relative to wherever the attack was actually aimed: Center means the aim point itself, Left/Right/High/Low mean whatever area is adjacent to the aim point in that direction on the target's own body chart, so aiming at the neck and rolling High lands on the head while aiming at the chest and rolling High lands on the neck instead — the same wedge, different area, because the aim moved. This means the real design needs an ADJACENCY relationship between areas on each body chart (what is above/below/beside what), not a static wedge table, plus an aim-point picker on the target diagram itself (the called-shot flow already lets a player name a specific area — this would be the same input, read spatially). **A slash is a separate case, not a fifth wedge:** it does not aim at one point at all, but sweeps a line of areas (the mockup approximated it as "every area in the aim point's row"), which is closer to the existing area-attack shapes (cone, bolt, cloud, glob) that already resolve to a SET of areas than to the single-target zone ladder — worth checking whether it belongs in that family rather than being invented fresh. Neither the adjacency model nor the slash shape is designed yet; this is a sharper statement of the open question, not an answer to it. **Confirmed with the developer 2026-09-17** — he is enthusiastic about it ("WOW! I am excite") and the scope grew one notch in the telling: the targets carry body forms, the to-hit dartboard maps onto the target for location, and damage is tracked **by type as well as by location**. Damage type is already resolved per blow (`MODE_DAMAGE_TYPES`, the creature attack's `damageType`, and the whole armour-blocking table keys off it), and `body.areas` already tracks wounds per area — what does not exist is wounds broken down by type within an area, which is a schema question to settle when this is designed rather than a rule to look up. **One more clarification, same walkthrough:** the wedge is never a player choice — only the aim point is. The zone still comes out of the natural/modified die roll against the current attack chart, exactly as `resolveAttack` already resolves it; the mockup's clickable wedges stood in for that roll outcome for demonstration only, not a control to actually build. |
| Equipment module (compendium-driven) | Done | **Closed 2026-09-18, and the "one gap" turned out not to exist in his code.** His `calcEncumbrance` never slows anyone down; `encumbrance_status` is only a label. What it DOES do, and the port was missing, is now ported into one shared `resolveEncumbrance` (combat-rules.mjs) that both actor models call: armour and gear scaled by the being's size (weapons exempt), magical items lightened (+1 x.9 to +10 x.05), and his own band labels ("Not encumbered" … "Over weight(cannot move)"), replacing the port's invented ones. The Player's Guide's movement penalty (3/4, 1/2, 1/4 with no running) is shown as a second "At current load" table **beside** his unencumbered rates, never replacing them. `UPSTREAM-ISSUES.md` item 31 asks whether it should replace them, and notes an unreachable branch and an undefined `tmp_weight` in his function. **Left for later:** quality tags and [Float] items, which need a field each (`docs/sonnet/2026-09-18-encumbrance.md`); Lighten Load / Spirit of the Donkey (deferred magic phase); fatigue (no system yet); the Config tab's equipment-handling modes (layout only in his sheet); and giant-material coverage. Suites: combat 406 (32 new), derivation 280, creature 140, availability 39; 27 modules parse. Checked in the sheet preview with a real 200 lb load. **Not verified:** anything needing a running Foundry V14. **Earlier on 2026-09-18, before this pass:** most of the DoD was already built. Weapon, armour and equipment item schemas exist (`item-weapon.mjs`, `item-armor.mjs`, `item-equipment.mjs`), with 594 weapons, 719 armour pieces and 636 equipment documents generated from his dictionaries. `_prepareEncumbrance` derives the Load Limit as the Strength table's `loadLimit` × body weight, with his four bands at ¼ / ½ / ¾ / whole (`changeAttribs`). It counts only equipped and carried items, skips tagalongs, and shows on the Attributes tab. Armour coverage, shields, penalties and the item sheet for armour came with combat. **Remaining:** (a) **the movement penalty for each encumbrance band is not applied**. The code says so explicitly (`prepareDerivedData`'s NOT YET IMPLEMENTED list), because the per-band penalty has not been read out of his code yet. That is the next concrete step. (b) His Config tab's Realistic / Loose / Free equipment handling, which only switches layout in his sheet, is not ported and may not need to be. (c) Giant-material coverage (`coverageFromMaterial`) is unresolved. A stale comment above `_prepareMovement` said equipment item types "do not exist yet" and has been corrected. Original DoD follows. Weapons/armor/gear schema; encumbrance/Load Limit tied to Strength per Attributes module. |

## Epic 4 — Creature/NPC (Layer 3)

| Story | Status | Definition of Done |
|---|---|---|
| Creature actor type & sheet | Done | Source-material blocker is resolved (Bestiary material obtained, Epic 0). Full audit of the Roll20 creature sheet (HTML lines 57576-92926) and the sheet-worker's creature-handling code (lines 174658-180370, plus shared functions that branch on creature-vs-character) is done and logged in `docs/DECISIONS.md`: "Creature/NPC audit findings", plus "CORRECTION: creature audit findings, checked against the source", which supersedes it wherever they differ. Its mechanical claims have been checked against the source. Reusable as-is: the 12 attributes and their saves, per-area wounds and armour (`createBodyAreas`; all 45 stock charts are already in `BODY_CHARTS`), and the attack zone ladder and fumble handling (`getAttackChart`/`resolveAttack`/`resolveFumble`). Creature-specific, needing new schema or logic: identity (type, subtype, level, life cycle, habitat, body type); a level-tiered attribute cap; characteristics with mixed derivation (Endurance and Hide entered, Shock defaulting to Endurance × 3, and Perception/Affinity/Fortune on a creature-only formula worked from the *as-built* attributes); entered resistances plus modifiers; a stored, editable body chart, not just a body type; the 10-slot natural-attack block (his own encoded format, not a weapon Item, with non-physical attack shapes and up to three effect riders), with to-hit modifiers summed the way `handlePhysicalAttacks` does rather than copied from his buggy creature path; abilities, disabilities and immunities as display-only on creatures (his racial copies do drive mechanics for characters); and Powers as innate spells/invocations with use counts, which can be stored now but not *used* until the Layer 4 spell/invocation engine exists. All three open design decisions and the model check-in are resolved (`DECISIONS.md`, "Creature schema: the three open design calls, resolved"): Abilities/Disabilities/Immunities are plain descriptive items; Powers and Attacks stay two separate item types; the tamed-creature/owner-relationship concept is deferred; the user picked **Fable** for the real implementation, then directed that it run on Opus, which is what happened. **Built:** `module/data/actor-creature.mjs` (entered Endurance, Hide, Shock and resistances; the creature-only Perception/Affinity/Fortune formula, with level counting double for Affinity and all three worked from the as-built ratings; the level-tiered attribute cap; a stored editable body chart; flat-chance skills, with `parseSkillList` for pasting a stat block); three new item types, `creatureAttack` (his `\|`/`@`/`^` encoding decoded into fields, with up to three rider effects), `power` and `trait` (ability, disability and immunity in one type); `module/combat/creature-rules.mjs` (attack-type behaviour, the touch rule, the four area shapes sized from Endurance, modifier gathering and rider triggers, all pure); `module/combat/creature-attack.mjs` with `templates/chat/creature-attack-card.hbs` (the roll, reusing the weapon card's Apply Damage and Spend Seconds wiring); and `module/sheets/actor-creature-sheet.mjs` with four tabs and five templates. Registered in `system.json`, `lang/en.json` and `module/imagine-rpg.mjs`, replacing the empty placeholder model. **Tested:** 121 creature tests pass (`tools/creature-test.html`) across the cap, the characteristics including every sense bonus, entered-versus-derived behaviour, resistances, body charts and wounds, the combat wiring, encumbrance and all of `creature-rules.mjs` — including the ceiling the port applies where his own helper drops it; the existing suites still pass unchanged (derivation 87, combat 101, availability 39); all 25 modules parse (`tools/syntax-check.html`); and the sheet's four tabs and the attack chat card were checked visually against a creature the real model derived (`tools/creature-preview.html`), with the numbers on screen verified by hand. **Not verified:** everything needing a running Foundry V14 — sheet registration, the attack dialog, the chat-card buttons, actor updates and initiative — since no V14 install exists on this machine. **Deferred:** a Power is stored and listed but cannot be resolved until the Layer 4 spell and invocation engine exists, and the EXP/CR budgeting functions, the evoke and Famorian body builders and the Agility jump table are not ported. See `DECISIONS.md`, "Creature/NPC implementation". |

## Epic 5 — Magic (Layer 4, deferred)

| Story | Status | Definition of Done |
|---|---|---|
| Base Magic (Aura) module | Backlog | Deferred until core phase complete. |
| Base Divine Magic (Piety) module | Backlog | Deferred until core phase complete. |
| Per-subsystem magic modules (runes, potions, bardic, etc.), individually toggleable + master off-switch | In Progress | **The content and the lore side are built (2026-09-22); what each use DOES is not.** Every kind of his Magic/Lore tab is an item answering to its own switch (the master switch and the sixteen subsystems already existed and now have content to switch), learned, used, brewed and memorized on his rules -- see the three rows below. Left: the worked-out effect of each use (his `do<Kind>Action` switches), the effects that change the character (Badger Thorn's Endurance and the like), the magic and divine item empowering sections, and mental combat and temporary modifiers, which his tab also carries. |
| Magic & Lore tab (content, rolls, memorization) | Done | **Built 2026-09-22, at the user's request, ahead of the Layer 4 deferral for the lore side only.** A sixth character-sheet tab (`templates/actor/tab-magic.hbs`, view `module/magic-view.mjs`, buttons `module/magic-actions.mjs`, rules `module/lore-rules.mjs`) over four new item types with their own sheets and four new packs. Consumables: Use takes a dose and the last removes the row (his `useHerb` and siblings); a poison is added from type, potency and form. Lores: a group per lore the character holds (learn or use skill) or has entries in, the rest under "Add other lore"; Use rolls the use skill (Intone/Recite/Sing for hymn/poem/song) with his floors, the 200% Grandmaster and the natural-100 fail, and needs the entry memorized; the picker rolls his learn check; recipes Brew a batch into the consumables. Memorization (goal + 1) x Knowledge's points, his lone-item allowance kept. Spells and invocations are known, memorized at their level and read out. Honours the magic switches and his No Intake rule. Tests: `tools/lore-test.html` (103), `tools/magic-preview.html`, window test (27). **Not verified:** anything needing a running Foundry V14. See `DECISIONS.md` 2026-09-22. |
| Starting lore (his "Provide random lore") | Done | **Built 2026-09-22.** `provideRandomLoreAndLoreItems` walked by `tools/extract/extract_lore_tables.py` and ported as `rollStartingLore`: an entry per instance of each lore skill in his count (racial rows plus the class list to title 10), hymns by alignment, poison recipes by alignment with their doses, potion recipes with 1d3, herbs by value plus the common Herb Lore roll, starting spells for his five casting skills with a primer on Affinity or Fortune. The generator's tick is on by default (his is off -- see DECISIONS) and the tab has a Game Master button; nothing is learned twice, doses stack. Departures reported as UPSTREAM 57 and 60; two names his lists draw that his tables lack are item 58. **Not verified:** the generator's create-then-give in a running V14. |
| Poison used on a victim | Done | **Built 2026-09-22 at the user's request.** His `doPoisonAction` as `resolvePoisonOnVictim` (`module/lore-rules.mjs`): immune, resisted or failed on the victim's own Poison Resistance roll, the type's clause, onset and duration rolled, and a per-interval Endurance damage rolled over the whole duration (labelled a convenience, not applied). `usePoison` in `module/magic-actions.mjs` asks targets / self / no one and a modifier. Durations in minutes by his row and the book (UPSTREAM 62). Tests in `tools/lore-test.html`. **Not built:** a poisoned weapon (coating), and applying the damage. |
| Lore use effects (his do-Action switches) | Backlog | **The next real piece of the magic work, and a judgement pass.** Every use handler of his ends in a `do<Kind>Action` switch -- ballads, candle lore, empathy, glyphs, hymns, poems, rituals, runes, songs, sympathy, herbs, potions, elixirs, charms -- writing the effect with the practitioner title worked in and dice rolled, and some changing the character. Until then a use posts the entry's own description and the practitioner title. Done when each kind's switch is ported (generated where the cases are regular), a success posts his worked text, and the effects that change a character are applied or listed for the Game Master. |
| Magic content attribution (book and page) | Backlog | All 2,486 documents in the four magic packs are XXX: `apply_sources` looks up by name and in these packs a name is not enough (rune Balance vs the Balance skill; spell Chill vs invocation Chill). Done when an attribution pass that knows the kind -- his Master Index's spell, herb and lore tables, and the books -- fills them, and `NOT_YET_ATTRIBUTED_PACKS` in `build_documents.py` is emptied. Mechanical once the lookup is scoped; see `docs/sonnet/2026-09-22-magic-lore-tab.md`. |

---

## Self-Check Checklist (run at the end of every work loop, before marking a story Done)

1. Does the implementation match the formula/table actually found in `docs/reference/`? Cite the page.
2. Does it conflict with the Roll20 sheet? If so, the sheet wins — note the deviation from the books here.
3. Is the story's Definition of Done fully met, not partially?
4. Update this file's status column.
5. Log any new architectural call in `docs/DECISIONS.md`.
6. Commit with a message describing what changed and why.

**The test routine.** Serve the repository over HTTP and open each suite; they stub Foundry, so
they run in any browser. As of 2026-09-22 the figures are:

| suite | file | checks |
|---|---|---|
| Combat rules | `tools/combat-test.html` | 511 |
| Derivation (character model) | `tools/derive-test.html` | 507 |
| Creature derivation | `tools/creature-test.html` | 157 |
| Advancement rules | `tools/advancement-test.html` | 101 |
| Character generation | `tools/chargen-test.html` | 83 |
| Content availability | `tools/availability-test.html` | 46 |
| Level-up walk (real writing code) | `tools/levelup-walk.html` | 37 |
| Equip rules | `tools/equip-test.html` | 21 |
| Importer (retired documents) | `tools/importer-test.html` | 9 |
| Changelog window | `tools/changelog-test.html` | 24 |
| Martial arts | `tools/martial-test.html` | 179 |
| Magic & Lore (rules, starting lore, the tab's view, poison on a victim) | `tools/lore-test.html` | 117 |
| Window chrome (every item sheet scrolls) | `tools/window-test.html` | 27 |
| Round clock (rules, view, the combat document through a whole fight) | `tools/round-test.html` | 87 |
| Manual-content loader (Python) | `python tools/extract/test_manual_content.py` | 122 |
| Weapon mods (his panel, the attack's extras and specials, temporary effects, the window) | `tools/weapon-mods-test.html` | 69 |
| Module parse check | `tools/syntax-check.html` | 71 modules |

**A plain `python -m http.server` lets the browser cache modules**, and on 2026-09-22 several
passes found a reloaded suite still running the previous copy of a module it imports. Serve with
`Cache-Control: no-store` (any small handler that sets it will do), or the numbers above can be a
stale file's.

```bash
python -m http.server 8777 --bind 127.0.0.1
```

The previews (`sheet-preview.html`, `item-preview.html`, `chargen-preview.html`,
`levelup-preview.html`, `creature-preview.html`, `availability-preview.html`) render the real
templates against real content and are worth a look whenever a template changes. A browser caches
ES modules hard; if a change seems not to have taken, serve with `Cache-Control: no-store` rather
than trusting the result.

## Epic 7 — Character Sheet UI

| Story | Status | Definition of Done |
|---|---|---|
| ApplicationV2 character sheet | In Progress | `module/sheets/actor-character-sheet.mjs` with header + attributes/skills/equipment tabs, each its own PART with a single root element as the framework requires. Renders correctly against real derived data (`tools/sheet-preview.html`). **Sheet registration API path not yet verified against a running Foundry V14** — no V14 install available on this machine. |
| Attribute save and skill roll handlers | In Progress | Implemented as `data-action` handlers producing chat messages with outcome tiers. Roll resolution follows his code for saves ("succeeded by half") and the Player's Guide for skills (±20% margin = critical). **Not yet exercised in Foundry.** |
| Remaining tabs (~~combat, magic, journal~~ → description) | Done | **Superseded in part 2026-09-22: the magic tab now exists** -- the Magic & Lore tab, see Epic 5 -- so "magic stays deferred" below is true of casting only. **Row corrected 2026-09-12; two thirds of it was already wrong**, and the real gap it found is now closed. The combat tab was already built and the event-time round already implemented; magic stays correctly deferred to Layer 4. **The description tab is built**: `templates/actor/tab-description.hbs`, a fifth `PART`/`TABS` entry on `ImagineCharacterSheet`, following the existing four exactly. It surfaces all **19 previously-unreachable scalar fields plus the languages array** — Physical Features (11 fields; `handedness` deliberately stays on the Combat tab, where it drives shield/off-hand resolution), Wealth (4 currencies + gems/jewelry/special), Languages (indexed Add/Delete rows, the same pattern the creature attack's rider effects use), and Tendencies. **Languages are wired end to end now**: the Intelligence-derived `spokenLanguages`/`writtenLanguages` allowance renders on the tab — it already existed on `attributes.int.mods` for free, since `_prepareAttributes` copies every column of the matching table row generically, so nothing new was derived, only surfaced; the cap's own enforcement still correctly belongs to the Attributes module (Epic 2, Backlog). **Superseded 2026-09-18:** the Attributes module built it, and the raw "0.25 spoken" line, which misread a partly spoken language as a fraction of a second one, is replaced by slots used against slots allowed, with his label on each row. Checked in `tools/sheet-preview.html` against a real derived character with every field given a genuine value, including the allowance line reading "4 spoken, 2 written" off her actual Intelligence. Suites unchanged throughout (combat 335, derivation 173, creature 129, availability 39 — expected, since nothing here is new derivation); 26 modules parse. Spec was `docs/sonnet/2026-09-12-description-tab.md`; full account in `DECISIONS.md` → "The description tab: 19 fields and a language list, made reachable". **Not verified:** anything needing a running Foundry V14 — the Add/Delete buttons writing back, and persistence in general. **The Skills tab now also shows class progression** — `getClassSkills(title)` on the class item, parallel to `getTitleName`, and three rows around the character's current title (one below, current, one above) when the class has anything recorded. Nothing shows for 85 of the 87 classes, since his `classtitledict` never carried skills-per-title, only title names; only classes hand-authored from a Word template (currently Elemental Dancer) populate it. Verified against the real Elemental Dancer document at title 7 in a second, isolated render on the sheet preview, rather than risking the main preview character's fixtures — full account in `DECISIONS.md` → "A character's Skills tab shows the class skills around its current title". |
| Item sheets | Done | **Six of nine done, and nine was never the target.** `module/sheets/item-sheet.mjs` gives creature attack, power, trait, class, armour and race their own sheets: one base class and six small subclasses, each naming its own body template, sharing `templates/item/item-header.hbs`. Class gets its three arrays as Add/Remove rows — titles paired with class skills by index, class modifiers uncapped (2 to 5 entries across the 87 classes extracted, and Monk's row is a column short of everyone else's, `UPSTREAM-ISSUES.md` item 1) — plus a 12-wide attribute-qualification grid. Armour gets its 19-location coverage grid laid out in the schema's own declared order (not alphabetical), paired left/right, with `coverageFromMaterial` shown read-only since the giant-material rule that resolves it isn't implemented yet. Race gets two 12-wide attribute rows, Mods above Limits under one shared header so the two — both twelve plain numbers, and `attributeLimits` is what `getAttributeMax` reads as a hard ceiling — can't be confused, plus editable walk/jog/run and special-movement tables. All three attribute grids share one `ATTRIBUTE_KEYS` constant (the Player's Guide's order) rather than three hand-typed copies. Verified in `tools/item-preview.html` against real extracted content chosen to exercise the interesting cases — Elemental Dancer (the one class with `classSkills` populated), Skin Suit(Watertight) and a real Buckler (full coverage grid and the shield flags), Elf(Sea) (special movement plus the -10 speed-multiplier case from `UPSTREAM-ISSUES.md` item 24) — every field round-trips; 26 modules parse; derive 164, combat 330, creature 129, availability 39 all unchanged throughout. **Not verified:** `Items.registerSheet` and `ItemSheetV2` follow the actor sheets' documented symmetry but have not been exercised in a running Foundry V14, same as the first three. **Weapon, skill and equipment stay on the default sheet on purpose** — broad but flat (equipment is nine plain fields), read far more often than written; the trigger to revisit is hand-authored content landing in `src/packs/manual/`, not field count. Rationale in `DECISIONS.md` → "Three of the six remaining item sheets, not six"; spec followed was `docs/sonnet/2026-09-12-item-sheets.md`. |
